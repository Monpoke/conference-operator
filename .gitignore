node_modules/
dist/
out/
# electron-builder's output (the room client's `directories.output`): a hundred
# megabytes or so of installer, regenerated by `package:win`.
release/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
data/
*.db
*.db-wal
*.db-shm

# HTML previews regenerated by `pnpm preview`: build output, not source.
preview/

# A room's runtime data: the paired machine's identity, recordings. They belong
# only to the machine that produced them.
.local-data/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

.out/

spikes/
