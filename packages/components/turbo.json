{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    /*
     * No output: the components are consumed as source by Vite, like every package
     * in this workspace. Only `typecheck` has a cost, and it goes through
     * `vue-tsc`, which pins this package to TypeScript 6.
     */
    "typecheck": {}
  }
}
