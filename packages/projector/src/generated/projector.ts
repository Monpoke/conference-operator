// Generated by `pnpm --filter @conference-operator/projector build`. Do not edit by hand.
// Regenerate after any change to src/browser or src/styles.

export const PROJECTOR_JS = "\"use strict\";\n(() => {\n  // packages/projector/src/browser/dom.ts\n  var cree = (balise, classe, texte) => {\n    const e = document.createElement(balise);\n    if (classe) e.className = classe;\n    if (texte != null) e.textContent = texte;\n    return e;\n  };\n  var contours = (racine) => {\n    racine.querySelectorAll(\".contour\").forEach((e) => {\n      e.dataset.texte = e.textContent ?? \"\";\n    });\n  };\n  var ecrire = (el, texte) => {\n    el.textContent = texte;\n    el.classList.remove(\"decoupe\");\n    delete el.dataset.decoupe;\n    if (el.classList.contains(\"contour\")) el.dataset.texte = texte;\n  };\n  function texteRiche(p, texte) {\n    let dernier = 0;\n    for (const m of texte.matchAll(/[#@][\\p{L}\\p{N}_]+/gu)) {\n      if (m.index > dernier) p.append(texte.slice(dernier, m.index));\n      p.append(cree(\"span\", \"tag\", m[0]));\n      dernier = m.index + m[0].length;\n    }\n    if (dernier < texte.length) p.append(texte.slice(dernier));\n  }\n  var initiales = (nom) => nom.split(/[\\s-]+/).filter(Boolean).slice(0, 2).map((m) => m[0].toUpperCase()).join(\"\");\n  var TEINTES = [\"#5e17eb\", \"#b01fd6\", \"#1f8fb0\", \"#2c2ca4\", \"#b44c70\", \"#c77414\"];\n  var teinte = (nom) => TEINTES[[...nom].reduce((h, c) => h * 31 + c.charCodeAt(0) >>> 0, 7) % TEINTES.length];\n  var pluriel = (n, mot) => `${n.toLocaleString(\"fr-FR\")} ${mot}${n > 1 ? \"s\" : \"\"}`;\n  function rond(s, i = 0) {\n    const r = cree(\"div\", \"rond flotte\");\n    r.style.setProperty(\"--i\", String(i));\n    if (s.echelle) r.style.setProperty(\"--e\", String(s.echelle));\n    if (s.logoUrl) {\n      const img = new Image();\n      img.src = s.logoUrl;\n      img.alt = s.nom || \"\";\n      img.addEventListener(\"error\", () => {\n        r.classList.add(\"sans-logo\");\n        r.replaceChildren(s.nom || \"\");\n      }, { once: true });\n      r.append(img);\n    } else {\n      r.classList.add(\"sans-logo\");\n      r.textContent = s.nom || \"\";\n    }\n    return r;\n  }\n  function svg(markup) {\n    if (!markup) return null;\n    const doc = new DOMParser().parseFromString(markup, \"image/svg+xml\");\n    const root = doc.documentElement;\n    if (root == null || root.nodeName.toLowerCase() !== \"svg\") return null;\n    return document.importNode(root, true);\n  }\n  function linkedin() {\n    const ns = \"http://www.w3.org/2000/svg\";\n    const icone = document.createElementNS(ns, \"svg\");\n    icone.setAttribute(\"viewBox\", \"0 0 64 64\");\n    icone.setAttribute(\"aria-hidden\", \"true\");\n    const fond = document.createElementNS(ns, \"rect\");\n    fond.setAttribute(\"width\", \"64\");\n    fond.setAttribute(\"height\", \"64\");\n    fond.setAttribute(\"rx\", \"12\");\n    fond.setAttribute(\"fill\", \"#fff\");\n    const lettres = document.createElementNS(ns, \"path\");\n    lettres.setAttribute(\"fill\", \"#0a66c2\");\n    lettres.setAttribute(\n      \"d\",\n      \"M14 24h8v26h-8zM18 11a4.6 4.6 0 1 1 0 9.2A4.6 4.6 0 0 1 18 11zM27 24h7.7v3.6h.1c1.1-2 3.7-4.2 7.6-4.2 8.1 0 9.6 5.3 9.6 12.3V50h-8V37.4c0-3 0-6.9-4.2-6.9s-4.8 3.3-4.8 6.7V50h-8z\"\n    );\n    icone.append(fond, lettres);\n    return icone;\n  }\n\n  // packages/projector/src/browser/time.ts\n  var formateurs = /* @__PURE__ */ new Map();\n  var formateur = (tz, options) => {\n    const cle = tz + JSON.stringify(options);\n    let f = formateurs.get(cle);\n    if (f == null) {\n      f = new Intl.DateTimeFormat(\"fr-FR\", { timeZone: tz, ...options });\n      formateurs.set(cle, f);\n    }\n    return f;\n  };\n  var maintenant = (data) => Date.now() + (data?.state.serverTimeOffsetMs ?? 0);\n  var heureDans = (ms, tz) => formateur(tz, { hour: \"2-digit\", minute: \"2-digit\", hourCycle: \"h23\" }).format(ms);\n  function dateLisible(valeur, now) {\n    if (!valeur) return \"\";\n    const t = Date.parse(valeur);\n    if (Number.isNaN(t) || !/^\\d{4}-\\d{2}-\\d{2}/.test(valeur)) return valeur;\n    const min = Math.round((now - t) / 6e4);\n    if (min < 1) return \"\\xE0 l'instant\";\n    if (min < 60) return `${min} min`;\n    const h = Math.round(min / 60);\n    return h < 24 ? `${h} h` : `${Math.round(h / 24)} j`;\n  }\n\n  // packages/projector/src/browser/barre-bas.ts\n  var BarreBas = class {\n    el = document.getElementById(\"barre-bas\");\n    q = (sel) => this.el.querySelector(sel);\n    libelle = this.q(\"[data-bb-libelle]\");\n    heure = this.q(\"[data-bb-heure]\");\n    titre = this.q(\"[data-bb-titre]\");\n    salle = this.q(\"[data-bb-salle]\");\n    message = this.q(\"[data-bb-message]\");\n    hashtag = this.q(\"[data-bb-hashtag]\");\n    horloge = this.q(\"[data-bb-horloge]\");\n    pause = this.q(\"#break-badge\");\n    lien = this.q(\"#status-dot\");\n    seconde = -1;\n    heureAffichee = \"\";\n    prochain = \"\";\n    maj(data, force = false) {\n      const conf = data.boucle?.barreBas;\n      const masquee = conf?.afficher === false;\n      document.body.classList.toggle(\"sans-barre-bas\", masquee);\n      if (masquee) {\n        this.el.hidden = true;\n        return;\n      }\n      this.el.hidden = false;\n      const now = maintenant(data);\n      const s = Math.floor(now / 1e3);\n      if (s === this.seconde && !force) return;\n      this.seconde = s;\n      const heure = heureDans(now, data.timezone);\n      if (heure !== this.heureAffichee) {\n        const [hh, mm] = heure.split(\":\");\n        this.horloge.replaceChildren(hh ?? \"\", cree(\"span\", \"deux-points\", \":\"), mm ?? \"\");\n        if (this.heureAffichee && typeof this.horloge.animate === \"function\") {\n          this.horloge.animate(\n            [{ opacity: 0, transform: \"translateY(-.5em)\" }, { opacity: 1, transform: \"none\" }],\n            { duration: 550, easing: \"cubic-bezier(.2, .9, .25, 1.2)\" }\n          );\n        }\n        this.heureAffichee = heure;\n      }\n      this.message.textContent = conf?.message ?? \"Partagez la journ\\xE9e avec\";\n      this.hashtag.textContent = conf?.hashtag ?? \"\";\n      const pause = data.state.breakBadge;\n      this.pause.hidden = pause == null;\n      if (pause != null) {\n        this.pause.textContent = pause.state === \"en-cours\" ? \"Pause\" : \"Pause \\xE0 venir\";\n        this.pause.classList.toggle(\"a-venir\", pause.state !== \"en-cours\");\n      }\n      this.lien.hidden = data.state.connectivity === \"ONLINE\";\n      const jour = data.agenda.length > 0 ? data.agenda : data.plannings.flatMap((p) => p.agenda.map((e) => ({ ...e, elsewhere: e.pause ? null : p.nom }))).sort((a, b) => a.startsAtMs - b.startsAtMs);\n      const suivante = jour.find((e) => e.startsAtMs > now && !e.pause);\n      const empreinte = suivante ? `${suivante.startsAtMs}|${suivante.title}|${suivante.elsewhere}` : \"rien\";\n      if (empreinte !== this.prochain || force) {\n        this.prochain = empreinte;\n        if (suivante) {\n          this.libelle.textContent = conf?.libelle ?? \"\\xC0 suivre\";\n          this.heure.textContent = heureDans(suivante.startsAtMs, data.timezone);\n          this.titre.textContent = suivante.title;\n          this.salle.textContent = suivante.elsewhere ?? \"\";\n          this.salle.hidden = !suivante.elsewhere;\n        } else {\n          this.libelle.textContent = \"\";\n          this.heure.textContent = \"\";\n          this.titre.textContent = jour.length ? conf?.finJournee ?? \"Merci et \\xE0 l'ann\\xE9e prochaine !\" : \"\";\n          this.salle.hidden = true;\n        }\n        const bloc = this.el.querySelector(\".bb-prochain\");\n        if (bloc && typeof bloc.animate === \"function\") {\n          bloc.animate(\n            [{ opacity: 0, transform: \"translateY(.45em)\" }, { opacity: 1, transform: \"none\" }],\n            { duration: 650, easing: \"cubic-bezier(.2, .9, .25, 1.15)\" }\n          );\n        }\n      }\n    }\n  };\n\n  // packages/projector/src/browser/effects.ts\n  var EFFETS = {\n    // A word arriving from the right, leaning and blurred, as if dragged in by the transition.\n    mot: {\n      images: [\n        { opacity: 0, transform: \"translateX(1.1em) skewX(-16deg)\", filter: \"blur(6px)\" },\n        { opacity: 1, transform: \"translateX(-.06em) skewX(4deg)\", filter: \"blur(0)\", offset: 0.7 },\n        { opacity: 1, transform: \"none\", filter: \"none\" }\n      ],\n      duree: 700,\n      easing: \"cubic-bezier(.2, .8, .3, 1)\"\n    },\n    // An agenda row, a card, a block: slides in from the right with a slight bounce.\n    entre: {\n      images: [{ opacity: 0, transform: \"translateX(120px)\" }, { opacity: 1, transform: \"none\" }],\n      duree: 700,\n      easing: \"cubic-bezier(.2, .9, .25, 1.12)\"\n    },\n    // Letters arriving from everywhere and assembling (drawn at random on every pass).\n    eclate: {\n      images: () => {\n        const x = (Math.random() - 0.5) * 14;\n        const y = (Math.random() - 0.5) * 8;\n        const r = (Math.random() - 0.5) * 400;\n        const e = 0.3 + Math.random() * 2.2;\n        return [\n          { opacity: 0, transform: `translate(${x.toFixed(2)}em, ${y.toFixed(2)}em) rotate(${r.toFixed(0)}deg) scale(${e.toFixed(2)})` },\n          { opacity: 1, offset: 0.35 },\n          { opacity: 1, transform: \"none\" }\n        ];\n      },\n      duree: 1100,\n      easing: \"cubic-bezier(.16, .84, .3, 1)\"\n    },\n    // Typewriter: each letter appears at once, in its turn.\n    machine: { images: [{ opacity: 0 }, { opacity: 1 }], duree: 30, easing: \"steps(1, end)\" },\n    // A word slamming onto the screen.\n    claque: {\n      images: [\n        { opacity: 0, transform: \"scale(3.2)\", filter: \"blur(8px)\" },\n        { opacity: 1, transform: \"scale(.94)\", filter: \"blur(0)\", offset: 0.6 },\n        { opacity: 1, transform: \"none\", filter: \"none\" }\n      ],\n      duree: 560,\n      easing: \"cubic-bezier(.4, 0, .5, 1)\"\n    },\n    monte: {\n      images: [{ opacity: 0, transform: \"translateY(.6em)\" }, { opacity: 1, transform: \"none\" }],\n      duree: 650,\n      easing: \"cubic-bezier(.2, .9, .25, 1.2)\"\n    },\n    lettre: {\n      images: [\n        { opacity: 0, transform: \"translateY(-.45em) scale(.2) rotate(-14deg)\" },\n        { opacity: 1, transform: \"translateY(0) scale(1.18) rotate(4deg)\", offset: 0.6 },\n        { opacity: 1, transform: \"none\" }\n      ],\n      duree: 560,\n      easing: \"ease-out\"\n    },\n    pop: {\n      images: [\n        { opacity: 0, transform: \"scale(.3)\" },\n        { opacity: 1, transform: \"scale(1.08)\", offset: 0.7 },\n        { opacity: 1, transform: \"scale(1)\" }\n      ],\n      duree: 560,\n      easing: \"ease-out\"\n    },\n    zoom: {\n      images: [\n        { opacity: 0, transform: \"scale(.72)\" },\n        { opacity: 1, transform: \"scale(1.03)\", offset: 0.75 },\n        { opacity: 1, transform: \"scale(1)\" }\n      ],\n      duree: 950,\n      easing: \"ease-out\"\n    },\n    glisse: {\n      images: [{ opacity: 0, transform: \"translateX(1.2em)\" }, { opacity: 1, transform: \"none\" }],\n      duree: 650,\n      easing: \"cubic-bezier(.2, .8, .2, 1)\"\n    },\n    tampon: {\n      images: [\n        { opacity: 0, transform: \"scale(2.4)\" },\n        { opacity: 1, transform: \"scale(.93)\", offset: 0.65 },\n        { opacity: 1, transform: \"scale(1)\" }\n      ],\n      duree: 500,\n      easing: \"cubic-bezier(.35, 0, .55, 1)\"\n    },\n    balayage: {\n      images: [{ clipPath: \"inset(-15% 100% -15% -5%)\" }, { clipPath: \"inset(-15% -5% -15% -5%)\" }],\n      duree: 950,\n      easing: \"cubic-bezier(.6, 0, .2, 1)\"\n    },\n    ecriture: {\n      images: [{ clipPath: \"inset(-40% 100% -40% -10%)\" }, { clipPath: \"inset(-40% -10% -40% -10%)\" }],\n      duree: 850,\n      easing: \"cubic-bezier(.5, 0, .3, 1)\"\n    }\n  };\n  var DECOUPES = {\n    mots: { mode: \"mots\", effet: \"mot\", pas: 70 },\n    claque: { mode: \"mots\", effet: \"claque\", pas: 170 },\n    lettres: { mode: \"lettres\", effet: \"lettre\", pas: 40 },\n    eclate: { mode: \"lettres\", effet: \"eclate\", pas: 22 },\n    machine: { mode: \"lettres\", effet: \"machine\", pas: 55 }\n  };\n  var DEBUT_EFFETS = { cut: 0, fade: 0.2, slide: 0.35, wipe: 0.3, zoom: 0.25 };\n  function decouper(el, mode) {\n    const texte = el.textContent ?? \"\";\n    const signature = `${mode}|${texte}`;\n    if (el.dataset.decoupe !== signature) {\n      const contour = el.classList.contains(\"contour\");\n      const morceaux = [];\n      let rang = 0;\n      for (const partie of texte.split(/(\\s+)/)) {\n        if (!partie) continue;\n        if (/^\\s+$/.test(partie)) {\n          morceaux.push(document.createTextNode(partie));\n          continue;\n        }\n        if (mode === \"lettres\") {\n          const mot = cree(\"span\", \"mot-lettres\");\n          for (const lettre of partie) {\n            const l = cree(\"span\", \"morceau\", lettre);\n            l.style.setProperty(\"--i\", String(rang++));\n            mot.append(l);\n          }\n          morceaux.push(mot);\n        } else {\n          const mot = cree(\"span\", contour ? \"morceau contour\" : \"morceau\", partie);\n          if (contour) mot.dataset.texte = partie;\n          mot.style.setProperty(\"--i\", String(rang++));\n          morceaux.push(mot);\n        }\n      }\n      el.replaceChildren(...morceaux);\n      if (contour) el.classList.add(\"decoupe\");\n      el.dataset.decoupe = signature;\n    }\n    return [...el.querySelectorAll(\".morceau\")];\n  }\n  var peutAnimer = () => typeof Element !== \"undefined\" && typeof Element.prototype.animate === \"function\";\n  function jouerEffets(racine, depart) {\n    const anime = peutAnimer();\n    for (const el of racine.querySelectorAll(\"[data-effet], [data-effet-enfants]\")) {\n      const delai = depart + (parseFloat(el.dataset.delai ?? \"\") || 0);\n      let cibles;\n      let nom;\n      let pas = parseFloat(el.dataset.pas ?? \"\");\n      if (el.dataset.effetEnfants) {\n        nom = el.dataset.effetEnfants;\n        cibles = [...el.dataset.cible ? el.querySelectorAll(el.dataset.cible) : el.children];\n        if (el.dataset.ordre === \"position\") {\n          const pos = new Map(cibles.map((c) => [c, c.getBoundingClientRect()]));\n          cibles.sort((a, b) => pos.get(a).top - pos.get(b).top || pos.get(a).left - pos.get(b).left);\n        }\n        if (Number.isNaN(pas)) pas = 80;\n      } else if (DECOUPES[el.dataset.effet ?? \"\"]) {\n        const d = DECOUPES[el.dataset.effet];\n        nom = d.effet;\n        cibles = decouper(el, d.mode);\n        if (Number.isNaN(pas)) pas = d.pas;\n      } else {\n        nom = el.dataset.effet ?? \"\";\n        cibles = [el];\n        pas = 0;\n      }\n      const effet = EFFETS[nom];\n      if (!anime || !effet || !cibles.length) continue;\n      pas = Math.min(pas, 2e3 / cibles.length);\n      cibles.forEach((c, i) => {\n        c.getAnimations().forEach((a2) => {\n          if (a2.id === \"effet\") a2.cancel();\n        });\n        const images = typeof effet.images === \"function\" ? effet.images() : effet.images;\n        const a = c.animate(images, { duration: effet.duree, delay: delai + i * pas, easing: effet.easing, fill: \"backwards\" });\n        a.id = \"effet\";\n      });\n    }\n  }\n\n  // packages/projector/src/browser/scenes/accueil.ts\n  function accueil(el) {\n    el.innerHTML = `\n    <div class=\"accueil\">\n      <p class=\"accueil-bienvenue vague\" data-effet=\"eclate\"></p>\n      <img class=\"accueil-logo flotte\" alt=\"\" data-effet=\"zoom\" data-delai=\"550\">\n    </div>`;\n    const texte = el.querySelector(\".accueil-bienvenue\");\n    const logo = el.querySelector(\".accueil-logo\");\n    return {\n      el,\n      cle: (data) => [data.boucle?.accueil.texte, data.boucle?.logoUrl ?? data.event?.logoUrl ?? null],\n      jouable: () => true,\n      rendre(data) {\n        ecrire(texte, data.boucle?.accueil.texte ?? \"Bienvenue \\xE0\");\n        const url = data.boucle?.logoUrl ?? data.event?.logoUrl ?? null;\n        if (url) {\n          if (logo.getAttribute(\"src\") !== url) logo.src = url;\n          logo.alt = data.eventIdentity.name;\n          logo.style.visibility = \"visible\";\n        } else {\n          logo.style.visibility = \"hidden\";\n        }\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/agenda.ts\n  var POLICE_MAX = 68;\n  var journeeIci = (data) => ({ nom: data.roomName ?? \"\", entries: data.agenda, ici: true });\n  var journeeAutre = (index) => (data) => {\n    const planning = data.plannings[index];\n    return planning == null ? null : { nom: planning.nom, entries: planning.agenda, ici: false };\n  };\n  function agenda(el, lire = journeeIci) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <div class=\"bande-titre\">\n        <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n        <span class=\"pastille pastille-ici\" data-ici data-effet=\"pop\" data-delai=\"500\" hidden>Vous \\xEAtes ici</span>\n      </div>\n      <p class=\"bande-droite\" data-horloge>--:--</p>\n    </header>\n    <div class=\"contenu agenda\" data-effet-enfants=\"entre\" data-cible=\".seance\" data-pas=\"60\" data-delai=\"200\">\n      <ol class=\"agenda-col\"></ol>\n      <ol class=\"agenda-col\"></ol>\n    </div>\n    <p class=\"vide\" hidden>Programme indisponible</p>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const ici = el.querySelector(\"[data-ici]\");\n    const horloge = el.querySelector(\"[data-horloge]\");\n    const zone = el.querySelector(\".agenda\");\n    const cols = [...el.querySelectorAll(\".agenda-col\")];\n    const vide = el.querySelector(\".vide\");\n    let toutes = [];\n    let sessions = [];\n    let lignes = [];\n    let nbTerminees = 0;\n    let heureAffichee = \"\";\n    let derniere = -1;\n    const masquer = (data) => data.boucle?.agenda.masquerTerminees ?? true;\n    function ligne(s, tz) {\n      const li = cree(\"li\", s.pause ? \"seance est-pause\" : \"seance\");\n      const heure = cree(\"div\", \"seance-heure\");\n      heure.append(cree(\"strong\", null, heureDans(s.startsAtMs, tz)));\n      heure.append(cree(\"span\", null, heureDans(s.endsAtMs, tz)));\n      const corps = cree(\"div\", \"seance-corps\");\n      corps.append(cree(\"p\", \"seance-titre\", s.title));\n      const qui = s.speakers.map((p) => p.company ? `${p.name} (${p.company})` : p.name).join(\", \");\n      const pastilles = [];\n      if (s.elsewhere) pastilles.push(cree(\"span\", \"pastille pastille-lieu\", s.elsewhere));\n      if (s.language) pastilles.push(cree(\"span\", \"pastille pastille-langue\", s.language));\n      if (qui || pastilles.length) {\n        const meta = cree(\"p\", \"seance-meta\");\n        meta.append(...pastilles);\n        if (qui) meta.append(qui);\n        corps.append(meta);\n      }\n      li.append(heure, corps, cree(\"i\", \"seance-avance\"));\n      return li;\n    }\n    function ajuster() {\n      const [A, B] = cols;\n      A.replaceChildren(...lignes);\n      B.replaceChildren();\n      const n = lignes.length;\n      if (!n) return;\n      const dispo = zone.clientHeight - 4;\n      const essai = (fs) => {\n        zone.style.setProperty(\"--fs\", `${fs}px`);\n        const gap = parseFloat(getComputedStyle(A).rowGap) || 0;\n        const h = lignes.map((li) => li.offsetHeight);\n        const total = h.reduce((a, b) => a + b, 0);\n        let meilleur = { k: n, max: Infinity };\n        let gauche = 0;\n        for (let k = 0; k <= n; k++) {\n          if (k) gauche += h[k - 1];\n          const hg = gauche + gap * Math.max(0, k - 1);\n          const hd = total - gauche + gap * Math.max(0, n - k - 1);\n          const max = Math.max(hg, hd);\n          if (max < meilleur.max) meilleur = { k, max };\n        }\n        return meilleur;\n      };\n      let bas = 12;\n      let haut = POLICE_MAX;\n      let retenu = null;\n      if (dispo > 0) {\n        for (let i = 0; i < 12; i++) {\n          const fs = (bas + haut) / 2;\n          const r = essai(fs);\n          if (r.max <= dispo) {\n            retenu = { fs, k: r.k };\n            bas = fs;\n          } else haut = fs;\n        }\n      }\n      if (!retenu) retenu = { fs: dispo > 0 ? 12 : 32, k: dispo > 0 ? essai(12).k : Math.ceil(n / 2) };\n      zone.style.setProperty(\"--fs\", `${retenu.fs.toFixed(2)}px`);\n      B.append(...lignes.slice(retenu.k));\n    }\n    const scene = {\n      el,\n      cle: (data) => [lire(data), data.timezone, masquer(data)],\n      jouable: (data) => (lire(data)?.entries.length ?? 0) > 0,\n      rendre(data) {\n        const journee = lire(data);\n        ecrire(titre, journee?.nom ?? \"\");\n        ici.hidden = !journee?.ici;\n        toutes = journee?.entries ?? [];\n        vide.hidden = toutes.length > 0;\n        const m = maintenant(data);\n        nbTerminees = toutes.filter((s) => m >= s.endsAtMs).length;\n        const restantes = toutes.filter((s) => m < s.endsAtMs);\n        sessions = masquer(data) && restantes.length ? restantes : toutes;\n        lignes = sessions.map((s) => ligne(s, data.timezone));\n        derniere = -1;\n        scene.tick(data, m);\n        ajuster();\n      },\n      tick(data, m) {\n        const secondes = Math.floor(m / 1e3);\n        if (secondes === derniere) return;\n        derniere = secondes;\n        const heure = heureDans(m, data.timezone);\n        if (heure !== heureAffichee) {\n          const [hh, mm] = heure.split(\":\");\n          horloge.replaceChildren(hh ?? \"\", cree(\"span\", \"deux-points\", \":\"), mm ?? \"\");\n          if (heureAffichee && typeof horloge.animate === \"function\") {\n            horloge.animate(\n              [{ opacity: 0, transform: \"translateY(-.55em)\" }, { opacity: 1, transform: \"none\" }],\n              { duration: 550, easing: \"cubic-bezier(.2, .9, .25, 1.2)\" }\n            );\n          }\n          heureAffichee = heure;\n        }\n        if (masquer(data) && toutes.filter((s) => m >= s.endsAtMs).length !== nbTerminees) scene.sale = true;\n        const suivante = sessions.findIndex((s) => s.startsAtMs > m && !s.pause);\n        sessions.forEach((s, i) => {\n          const li = lignes[i];\n          const enCours = m >= s.startsAtMs && m < s.endsAtMs;\n          li.classList.toggle(\"est-passee\", m >= s.endsAtMs);\n          li.classList.toggle(\"est-encours\", enCours);\n          li.classList.toggle(\"est-suivante\", i === suivante);\n          li.style.setProperty(\"--avance\", enCours ? ((m - s.startsAtMs) / (s.endsAtMs - s.startsAtMs)).toFixed(4) : \"0\");\n        });\n      }\n    };\n    return scene;\n  }\n\n  // packages/projector/src/browser/scenes/sponsors.ts\n  var ZOOM_SPONSORS_MAX = 1.1;\n  function annonce(el, index) {\n    el.innerHTML = `\n    <div class=\"annonce\">\n      <p class=\"annonce-titre f-titre contour vague\" data-titre data-effet=\"mots\"></p>\n      <p class=\"annonce-sous f-titre contour\" data-sous-titre data-effet=\"claque\" data-delai=\"500\"></p>\n      <div class=\"annonce-logos\" data-logos data-effet-enfants=\"pop\" data-delai=\"750\"></div>\n    </div>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const sous = el.querySelector(\"[data-sous-titre]\");\n    const logos = el.querySelector(\"[data-logos]\");\n    const donnees = (data) => data.boucle?.annonces[index] ?? null;\n    return {\n      el,\n      cle: donnees,\n      jouable: (data) => (donnees(data)?.logos.length ?? 0) > 0,\n      rendre(data) {\n        const a = donnees(data);\n        ecrire(titre, a?.titre ?? \"\");\n        ecrire(sous, a?.sousTitre ?? \"\");\n        logos.replaceChildren(...(a?.logos ?? []).map((logo, i) => rond(logo, i)));\n      }\n    };\n  }\n  function merci(el) {\n    el.innerHTML = `\n    <div class=\"titre-plein\">\n      <h2 class=\"f-titre contour vague\" data-effet=\"claque\"></h2>\n    </div>`;\n    const titre = el.querySelector(\"h2\");\n    return {\n      el,\n      cle: (data) => data.boucle?.merciSponsors ?? null,\n      // Thanking sponsors nobody then shows would read as a mistake.\n      jouable: (data) => Boolean(data.boucle?.merciSponsors) && (data.boucle?.sponsorPages ?? []).some((page) => page.rangs.some((row) => row.logos.length > 0)),\n      rendre(data) {\n        ecrire(titre, data.boucle?.merciSponsors ?? \"\");\n      }\n    };\n  }\n  function sponsors(el, index) {\n    el.innerHTML = `\n    <div class=\"sp\">\n      <h2 class=\"sp-titre f-titre vague\" data-titre data-effet=\"mots\"></h2>\n      <div class=\"sp-zone\"><div class=\"sp-rangs\" data-effet-enfants=\"pop\" data-cible=\".rond\" data-pas=\"90\" data-delai=\"300\"></div></div>\n    </div>\n    <p class=\"vide\" hidden>Nos partenaires</p>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const zone = el.querySelector(\".sp-zone\");\n    const contenu = el.querySelector(\".sp-rangs\");\n    const vide = el.querySelector(\".vide\");\n    const donnees = (data) => data.boucle?.sponsorPages[index] ?? null;\n    return {\n      el,\n      cle: donnees,\n      jouable: (data) => (donnees(data)?.rangs ?? []).some((row) => row.logos.length > 0),\n      rendre(data) {\n        const page = donnees(data) ?? { titre: \"\", rangs: [] };\n        ecrire(titre, page.titre);\n        const rangs = page.rangs.filter((row) => row.logos.length > 0);\n        vide.hidden = rangs.length > 0;\n        let i = 0;\n        contenu.replaceChildren(...rangs.map((row) => {\n          const rang = cree(\"div\", \"sp-rang\");\n          rang.style.setProperty(\"--t\", String(row.taille || 1));\n          rang.append(...row.logos.map((logo) => rond(logo, i++)));\n          return rang;\n        }));\n        const marge = 30;\n        let bas = 0.2;\n        let haut = ZOOM_SPONSORS_MAX;\n        let ok = zone.clientHeight > 0 ? bas : 1;\n        if (zone.clientHeight > 0) {\n          for (let n = 0; n < 12; n++) {\n            const k = (bas + haut) / 2;\n            zone.style.setProperty(\"--k\", k.toFixed(3));\n            if (contenu.offsetHeight <= zone.clientHeight - marge) {\n              ok = k;\n              bas = k;\n            } else haut = k;\n          }\n        }\n        zone.style.setProperty(\"--k\", ok.toFixed(3));\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/mur.ts\n  function mur(el) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n      <p class=\"bande-droite\" data-hashtag data-effet=\"glisse\" data-delai=\"350\"></p>\n    </header>\n    <div class=\"contenu mur\" data-effet-enfants=\"entre\" data-cible=\".carte\" data-ordre=\"position\" data-pas=\"110\" data-delai=\"250\">\n      <div class=\"mur-col\"></div>\n      <div class=\"mur-col\"></div>\n      <div class=\"mur-col\"></div>\n    </div>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const hashtag = el.querySelector(\"[data-hashtag]\");\n    const cols = [...el.querySelectorAll(\".mur-col\")];\n    let file = null;\n    let posts = [];\n    let now = 0;\n    function carte(p) {\n      const auteur = p.auteur || \"\";\n      const article = cree(\"article\", \"carte\");\n      const tete = cree(\"header\", \"carte-tete\");\n      const avatar = cree(\"div\", \"avatar\");\n      if (p.photoUrl) {\n        const img = new Image();\n        img.src = p.photoUrl;\n        img.alt = \"\";\n        avatar.append(img);\n      } else {\n        avatar.textContent = initiales(auteur || \"?\");\n        avatar.style.setProperty(\"--c\", teinte(auteur || \"?\"));\n      }\n      const qui = cree(\"div\");\n      qui.append(cree(\"p\", \"carte-nom\", auteur));\n      if (p.titre) qui.append(cree(\"p\", \"carte-titre\", p.titre));\n      tete.append(avatar, qui, cree(\"p\", \"carte-date\", dateLisible(p.date, now)));\n      const texte = cree(\"p\", \"carte-texte\");\n      texteRiche(texte, p.texte || \"\");\n      article.append(tete, texte);\n      if (p.imageUrl) {\n        article.classList.add(\"avec-image\");\n        const img = new Image();\n        img.src = p.imageUrl;\n        img.alt = \"\";\n        img.className = \"carte-image\";\n        article.append(img);\n      }\n      const stats = [];\n      if (p.reactions) stats.push(pluriel(p.reactions, \"r\\xE9action\"));\n      if (p.commentaires) stats.push(pluriel(p.commentaires, \"commentaire\"));\n      const pied = cree(\"footer\", \"carte-pied\");\n      pied.append(cree(\"span\", null, stats.join(\", \")), cree(\"span\", null, p.reseau || \"LinkedIn\"));\n      article.append(pied);\n      return article;\n    }\n    function page() {\n      cols.forEach((c) => c.replaceChildren());\n      if (!posts.length) return;\n      if (!file || file.length !== posts.length) file = posts.map((_, i) => i);\n      const bas = (c) => {\n        const d = c.lastElementChild;\n        return d ? d.offsetTop + d.offsetHeight : 0;\n      };\n      const places = [];\n      let echecs = 0;\n      for (const i of file) {\n        if (echecs >= 4) break;\n        const col = cols.reduce((a, c2) => bas(c2) < bas(a) ? c2 : a);\n        const c = carte(posts[i]);\n        col.append(c);\n        if (places.length && col.clientHeight > 0 && bas(col) > col.clientHeight) {\n          c.remove();\n          echecs++;\n          continue;\n        }\n        places.push(i);\n      }\n      file = file.filter((i) => !places.includes(i)).concat(places);\n    }\n    return {\n      el,\n      cle: (data) => data.boucle?.mur ?? null,\n      jouable: (data) => (data.boucle?.mur.posts.length ?? 0) > 0,\n      rendre(data) {\n        ecrire(titre, data.boucle?.mur.titre || \"Ils en parlent\");\n        ecrire(hashtag, data.boucle?.mur.hashtag ?? \"\");\n        posts = data.boucle?.mur.posts ?? [];\n        now = maintenant(data);\n        file = null;\n        page();\n      },\n      quitte(data) {\n        now = maintenant(data);\n        page();\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/message.ts\n  function message(el, cle) {\n    el.innerHTML = `\n    <div class=\"message\">\n      <p class=\"message-texte f-titre vague\" data-texte-message></p>\n      <p class=\"message-sous\" data-sous data-effet=\"machine\" data-delai=\"900\"></p>\n    </div>`;\n    const texte = el.querySelector(\"[data-texte-message]\");\n    const sous = el.querySelector(\"[data-sous]\");\n    const donnees = (data) => data.boucle?.messages[cle] ?? null;\n    return {\n      el,\n      cle: donnees,\n      // Silencing phones speaks to a room about to hear a talk: the global screen —\n      // a hall, with no talk of its own — has no one to ask.\n      jouable: (data) => Boolean(donnees(data)?.texte) && !(cle === \"silence\" && data.state.roomId == null),\n      rendre(data) {\n        const m = donnees(data);\n        const effet = m?.effet ?? \"claque\";\n        const parLettres = DECOUPES[effet]?.mode === \"lettres\";\n        texte.classList.toggle(\"contour\", !parLettres);\n        texte.classList.toggle(\"arcenciel\", parLettres);\n        texte.dataset.effet = effet;\n        ecrire(texte, m?.texte ?? \"\");\n        ecrire(sous, m?.sousTitre ?? \"\");\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/salles.ts\n  function salles(el) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n    </header>\n    <div class=\"contenu salles\" data-effet-enfants=\"entre\" data-cible=\".carte\" data-pas=\"140\" data-delai=\"250\"></div>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const zone = el.querySelector(\".salles\");\n    const occupees = (data) => data.otherRooms.filter((room) => room.session != null);\n    return {\n      el,\n      cle: (data) => [data.otherRooms, data.timezone, data.state.roomId],\n      // The other rooms' schedules already show what is on there: this scene only\n      // plays where they are not shown.\n      jouable: (data) => occupees(data).length > 0 && data.plannings.length === 0,\n      rendre(data) {\n        ecrire(titre, data.state.roomId == null ? \"En ce moment dans les salles\" : \"Pendant ce temps, \\xE0 c\\xF4t\\xE9\");\n        const rooms = occupees(data);\n        zone.style.setProperty(\"--n\", String(rooms.length > 2 ? 2 : Math.max(1, rooms.length)));\n        zone.replaceChildren(...rooms.map((room) => {\n          const session = room.session;\n          const carte = cree(\"article\", \"carte salle\");\n          const tete = cree(\"header\", \"carte-tete\");\n          tete.append(\n            cree(\"p\", \"salle-nom\", room.name),\n            cree(\n              \"p\",\n              room.running ? \"salle-heure maintenant\" : \"salle-heure\",\n              room.running ? \"En ce moment\" : heureDans(Date.parse(session.startsAt), data.timezone)\n            )\n          );\n          carte.append(tete, cree(\"p\", \"salle-titre\", session.title));\n          if (session.speakers.length) carte.append(cree(\"p\", \"salle-qui\", session.speakers.join(\", \")));\n          return carte;\n        }));\n      }\n    };\n  }\n  function reseaux(el) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n      <p class=\"bande-droite\" data-hashtag data-effet=\"glisse\" data-delai=\"350\"></p>\n    </header>\n    <div class=\"contenu reseaux\" data-effet-enfants=\"pop\" data-cible=\".carte\" data-pas=\"120\" data-delai=\"300\"></div>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const hashtag = el.querySelector(\"[data-hashtag]\");\n    const zone = el.querySelector(\".reseaux\");\n    return {\n      el,\n      cle: (data) => [data.socialLinks, data.eventIdentity.shortName, data.boucle?.barreBas.hashtag],\n      jouable: (data) => data.socialLinks.length > 0,\n      rendre(data) {\n        ecrire(titre, `Suivez ${data.eventIdentity.shortName}`.trim());\n        ecrire(hashtag, data.boucle?.barreBas.hashtag ?? \"\");\n        zone.replaceChildren(...data.socialLinks.map((link) => {\n          const carte = cree(\"article\", \"carte reseau\");\n          carte.append(cree(\"p\", \"reseau-nom\", link.network), cree(\"p\", \"reseau-compte\", link.handle));\n          return carte;\n        }));\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/wallsio.ts\n  function wallsio(el) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n      <p class=\"bande-droite\" data-hashtag data-effet=\"glisse\" data-delai=\"350\"></p>\n    </header>\n    <div class=\"contenu wallsio\" data-effet=\"entre\" data-delai=\"200\"></div>\n    <p class=\"vide\" hidden>Aucun mur configur\\xE9 sur le hub.</p>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const hashtag = el.querySelector(\"[data-hashtag]\");\n    const zone = el.querySelector(\".wallsio\");\n    const vide = el.querySelector(\".vide\");\n    let chargeA = 0;\n    let pret = false;\n    function charger(src) {\n      pret = false;\n      chargeA = Date.now();\n      zone.dataset.url = src;\n      const cadre = document.createElement(\"iframe\");\n      cadre.title = \"Mur social\";\n      cadre.loading = \"eager\";\n      cadre.addEventListener(\"load\", () => {\n        pret = true;\n      }, { once: true });\n      setTimeout(() => {\n        pret = true;\n      }, 15e3);\n      cadre.src = src;\n      zone.replaceChildren(cadre);\n    }\n    const scene = {\n      el,\n      cle: (data) => [data.boucle?.wallsio, data.wallsIoReachable],\n      // The iframe exists as soon as there is an address: the page loads it off\n      // screen and plays the scene once walls.io answers.\n      jouable: (data) => Boolean(data.boucle?.wallsio.src) && data.wallsIoReachable && pret,\n      rendre(data) {\n        const c = data.boucle?.wallsio;\n        ecrire(titre, c?.titre ?? \"\");\n        ecrire(hashtag, c?.hashtag ?? \"\");\n        zone.style.setProperty(\"--zoom-wallsio\", String(Math.max(0.5, c?.zoom ?? 1)));\n        const src = c?.src ?? null;\n        vide.hidden = src != null;\n        if (src == null) {\n          zone.replaceChildren();\n          delete zone.dataset.url;\n          return;\n        }\n        const minutes = c?.rechargeMinutes ?? 0;\n        const expire = minutes > 0 && chargeA > 0 && Date.now() - chargeA > minutes * 6e4;\n        if (data.wallsIoReachable && (zone.dataset.url !== src || expire)) charger(src);\n      },\n      tick(data) {\n        const minutes = data.boucle?.wallsio.rechargeMinutes ?? 0;\n        if (minutes > 0 && chargeA > 0 && Date.now() - chargeA > minutes * 6e4) scene.sale = true;\n      }\n    };\n    return scene;\n  }\n\n  // packages/projector/src/browser/scenes/affiches.ts\n  var CADRE = `<svg viewBox=\"0 0 100 100\" fill=\"none\" stroke=\"#fff\" stroke-width=\"3.2\" stroke-linecap=\"round\">\n  <path d=\"M3 28V13Q3 3 13 3H28M72 3H87Q97 3 97 13V28M97 72V87Q97 97 87 97H72M28 97H13Q3 97 3 87V72\"/>\n</svg>`;\n  function poserQr(cadre, markup) {\n    const qr = cadre.querySelector(\".qr\");\n    const node = markup ? svg(markup) : null;\n    qr.replaceChildren(...node ? [node] : []);\n    cadre.style.visibility = node ? \"visible\" : \"hidden\";\n  }\n  function conduite(el) {\n    el.innerHTML = `\n    <div class=\"cc-panneau\"></div>\n    <div class=\"cc-qr\">\n      <p class=\"cc-legende\" data-effet=\"machine\" data-delai=\"1000\"></p>\n      <div class=\"cadre-qr respire\" data-effet=\"pop\" data-delai=\"1150\">${CADRE}<div class=\"qr\"></div></div>\n    </div>\n    <div class=\"slogan cc-slogan\">\n      <p class=\"slogan-creux\" data-effet=\"tampon\" data-delai=\"1400\"></p>\n      <p class=\"slogan-orange contour respire\" data-effet=\"tampon\" data-delai=\"1600\"></p>\n    </div>`;\n    const panneau = el.querySelector(\".cc-panneau\");\n    const legende = el.querySelector(\".cc-legende\");\n    const cadre = el.querySelector(\".cadre-qr\");\n    const creux = el.querySelector(\".slogan-creux\");\n    const orange = el.querySelector(\".slogan-orange\");\n    return {\n      el,\n      cle: (data) => data.boucle?.conduite ?? null,\n      jouable: (data) => (data.boucle?.conduite.paragraphes.length ?? 0) > 0,\n      rendre(data) {\n        const c = data.boucle?.conduite;\n        panneau.replaceChildren(...(c?.paragraphes ?? []).map((texte, i) => {\n          const p = cree(\"p\", \"contour\", texte);\n          p.dataset.texte = texte;\n          p.dataset.effet = \"balayage\";\n          if (i) p.dataset.delai = String(i * 350);\n          return p;\n        }));\n        ecrire(legende, c?.qrSvg ? c.legende : \"\");\n        poserQr(cadre, c?.qrSvg ?? null);\n        ecrire(creux, c?.slogan.creux ?? \"\");\n        ecrire(orange, c?.slogan.orange ?? \"\");\n      }\n    };\n  }\n  function feedbacks(el) {\n    el.innerHTML = `\n    <div class=\"slogan fb-slogan\">\n      <p class=\"slogan-creux\" data-effet=\"tampon\"></p>\n      <p class=\"slogan-orange contour respire\" data-effet=\"tampon\" data-delai=\"200\"></p>\n    </div>\n    <div class=\"fb-qr cadre-qr respire\" data-effet=\"pop\" data-delai=\"450\">${CADRE}<div class=\"qr\"></div></div>\n    <p class=\"fb-merci\"><span class=\"balance\" data-effet=\"ecriture\" data-delai=\"800\"></span><span class=\"balance\" style=\"--i:1\" data-effet=\"ecriture\" data-delai=\"1350\"></span></p>`;\n    const creux = el.querySelector(\".slogan-creux\");\n    const orange = el.querySelector(\".slogan-orange\");\n    const cadre = el.querySelector(\".fb-qr\");\n    const [merci1, merci2] = [...el.querySelectorAll(\".fb-merci span\")];\n    return {\n      el,\n      cle: (data) => data.boucle?.feedbacks ?? null,\n      // Asking for feedback with nowhere to give it would be a dead end.\n      jouable: (data) => Boolean(data.boucle?.feedbacks.qrSvg),\n      rendre(data) {\n        const f = data.boucle?.feedbacks;\n        ecrire(creux, f?.slogan.creux ?? \"\");\n        ecrire(orange, f?.slogan.orange ?? \"\");\n        poserQr(cadre, f?.qrSvg ?? null);\n        ecrire(merci1, f?.merci[0] ?? \"\");\n        ecrire(merci2, f?.merci[1] ?? \"\");\n      }\n    };\n  }\n\n  // packages/projector/src/browser/scenes/operateur.ts\n  function finEffective(sessions, index) {\n    const s = sessions[index];\n    if (s == null) return null;\n    if (s.endsAtMs != null) return s.endsAtMs;\n    if (s.durationMinutes != null) return s.startsAtMs + s.durationMinutes * 6e4;\n    return sessions[index + 1]?.startsAtMs ?? null;\n  }\n  function programme(el) {\n    el.innerHTML = `\n    <header class=\"bande\">\n      <div class=\"bande-titre\">\n        <h2 class=\"f-titre\" data-titre data-effet=\"mots\"></h2>\n        <span class=\"pastille pastille-ici\" data-effet=\"pop\" data-delai=\"500\">Vous \\xEAtes ici</span>\n      </div>\n      <p class=\"bande-droite\" data-horloge>--:--</p>\n    </header>\n    <div class=\"contenu scroller\"><ol class=\"programme-liste\" data-effet-enfants=\"entre\" data-cible=\".seance\" data-pas=\"40\" data-delai=\"200\"></ol></div>\n    <p class=\"vide\" hidden>Programme indisponible</p>`;\n    const titre = el.querySelector(\"[data-titre]\");\n    const horloge = el.querySelector(\"[data-horloge]\");\n    const cadre = el.querySelector(\".scroller\");\n    const liste = el.querySelector(\".programme-liste\");\n    const vide = el.querySelector(\".vide\");\n    function cycle() {\n      liste.classList.remove(\"cycling\");\n      const hauteur = cadre.clientHeight;\n      const course = liste.scrollHeight - hauteur;\n      const ancre = liste.querySelector(\".anchor\");\n      ancre?.scrollIntoView?.({ block: \"center\" });\n      if (!(course > 0)) return;\n      const vise = ancre ? ancre.offsetTop - (hauteur - ancre.offsetHeight) / 2 : 0;\n      const depart = Math.max(0, Math.min(vise, course));\n      cadre.scrollTop = 0;\n      liste.style.setProperty(\"--from\", `${-depart}px`);\n      liste.style.setProperty(\"--end\", `${-course}px`);\n      liste.style.animationDuration = `${Math.round(course / hauteur * 22e3 + 12e3)}ms`;\n      liste.classList.add(\"cycling\");\n    }\n    return {\n      el,\n      cle: (data) => [data.sessions, data.roomName, data.state.currentSession?.id, data.state.nextSession?.id],\n      jouable: () => true,\n      vide: \"Programme indisponible\",\n      rendre(data) {\n        ecrire(titre, data.roomName ?? \"\");\n        const now = maintenant(data);\n        const running = data.state.currentSession?.id;\n        const anchor = running ?? data.state.nextSession?.id;\n        vide.hidden = data.sessions.length > 0;\n        liste.replaceChildren(...data.sessions.map((s, index) => {\n          const end = finEffective(data.sessions, index);\n          const classes = [\"seance\"];\n          const pause = s.kind === \"break\" && ((s.roomSpan ?? 1) > 1 || s.sharedFrom != null);\n          if (pause) classes.push(\"est-pause\");\n          if (s.id === running) classes.push(\"est-encours\");\n          else if (end != null && end < now) classes.push(\"est-passee\");\n          if (s.id === anchor) classes.push(\"anchor\");\n          const li = cree(\"li\", classes.join(\" \"));\n          const heure = cree(\"div\", \"seance-heure\");\n          heure.append(cree(\"strong\", null, heureDans(s.startsAtMs, data.timezone)));\n          if (end != null) heure.append(cree(\"span\", null, heureDans(end, data.timezone)));\n          const corps = cree(\"div\", \"seance-corps\");\n          corps.append(cree(\"p\", \"seance-titre\", s.title));\n          const qui = s.speakers.map((p) => p.company ? `${p.name} (${p.company})` : p.name).join(\", \");\n          if (qui) corps.append(cree(\"p\", \"seance-meta\", qui));\n          li.append(heure, corps);\n          return li;\n        }));\n        cycle();\n      },\n      tick(data, now) {\n        horloge.textContent = heureDans(now, data.timezone);\n      }\n    };\n  }\n  function decompte(el) {\n    el.innerHTML = `\n    <div class=\"decompte\">\n      <p class=\"decompte-titre f-titre contour\" data-effet=\"mots\">Reprise dans</p>\n      <p class=\"decompte-chiffres f-titre contour\"><span class=\"cd-min\">--</span>:<span class=\"cd-sec\">--</span></p>\n      <p class=\"decompte-reprise\" data-effet=\"glisse\" data-delai=\"300\"></p>\n    </div>`;\n    const titre = el.querySelector(\".decompte-titre\");\n    const chiffres = el.querySelector(\".decompte-chiffres\");\n    const min = el.querySelector(\".cd-min\");\n    const sec = el.querySelector(\".cd-sec\");\n    const reprise = el.querySelector(\".decompte-reprise\");\n    let derniere = null;\n    return {\n      el,\n      cle: (data) => data.state.nextSession?.id ?? null,\n      jouable: () => true,\n      rendre(data) {\n        const next = data.state.nextSession;\n        chiffres.hidden = next == null;\n        ecrire(titre, next ? \"Reprise dans\" : \"Fin des interventions\");\n        ecrire(reprise, next ? `Reprise \\u2014 ${next.title}` : \"\");\n      },\n      tick(data, now) {\n        const next = data.state.nextSession;\n        if (next == null) return;\n        const reste = Math.max(0, next.startsAtMs - now);\n        const secondes = Math.floor(reste % 6e4 / 1e3);\n        min.textContent = String(Math.floor(reste / 6e4)).padStart(2, \"0\");\n        sec.textContent = String(secondes).padStart(2, \"0\");\n        chiffres.dataset.texte = chiffres.textContent ?? \"\";\n        if (secondes !== derniere) {\n          derniere = secondes;\n          chiffres.classList.remove(\"beat\");\n          void chiffres.offsetWidth;\n          chiffres.classList.add(\"beat\");\n        }\n      }\n    };\n  }\n  function banniere(el) {\n    el.innerHTML = `\n    <div class=\"message\">\n      <p class=\"message-texte f-titre contour vague\" data-effet=\"claque\"></p>\n    </div>`;\n    const cadre = el.querySelector(\".message\");\n    const texte = el.querySelector(\".message-texte\");\n    return {\n      el,\n      cle: (data) => data.state.message,\n      jouable: () => true,\n      rendre(data) {\n        const m = data.state.message;\n        cadre.classList.toggle(\"urgent\", m?.level === \"urgent\");\n        cadre.classList.toggle(\"warning\", m?.level === \"warning\");\n        ecrire(texte, m?.text ?? \"\\u2014\");\n      }\n    };\n  }\n  function avis(el) {\n    el.innerHTML = `\n    <div class=\"avis\">\n      <div class=\"cadre-qr respire\" data-effet=\"pop\" data-delai=\"300\">\n        <svg viewBox=\"0 0 100 100\" fill=\"none\" stroke=\"#fff\" stroke-width=\"3.2\" stroke-linecap=\"round\"><path d=\"M3 28V13Q3 3 13 3H28M72 3H87Q97 3 97 13V28M97 72V87Q97 97 87 97H72M28 97H13Q3 97 3 87V72\"/></svg>\n        <div class=\"qr\"></div>\n      </div>\n      <div>\n        <h2 class=\"avis-titre f-titre contour\" data-effet=\"mots\">Votre avis sur cette conf\\xE9rence</h2>\n        <p class=\"avis-talk\" data-effet=\"glisse\" data-delai=\"400\"></p>\n        <p class=\"avis-aide\" data-effet=\"glisse\" data-delai=\"600\">Scannez pour noter la conf\\xE9rence et laisser un commentaire aux speakers.</p>\n      </div>\n    </div>\n    <p class=\"vide\" hidden>Aucune conf\\xE9rence \\xE0 noter</p>`;\n    const cadre = el.querySelector(\".avis\");\n    const qr = el.querySelector(\".qr\");\n    const talk = el.querySelector(\".avis-talk\");\n    const vide = el.querySelector(\".vide\");\n    return {\n      el,\n      cle: (data) => [data.feedback, data.state.currentSession?.title],\n      jouable: () => true,\n      rendre(data) {\n        const node = data.feedback?.qrSvg ? svg(data.feedback.qrSvg) : null;\n        cadre.hidden = node == null;\n        vide.hidden = node != null;\n        qr.replaceChildren(...node ? [node] : []);\n        ecrire(talk, data.state.currentSession?.title ?? \"\");\n      }\n    };\n  }\n  function question(el) {\n    el.innerHTML = `\n    <header class=\"bande\"><h2 class=\"f-titre\" data-effet=\"mots\">Question du public</h2></header>\n    <div class=\"question\" data-effet=\"balayage\" data-delai=\"250\">\n      <p class=\"question-texte contour\"></p>\n      <p class=\"question-auteur\"></p>\n    </div>\n    <p class=\"vide\" hidden>Aucune question affich\\xE9e</p>`;\n    const panneau = el.querySelector(\".question\");\n    const texte = el.querySelector(\".question-texte\");\n    const auteur = el.querySelector(\".question-auteur\");\n    const vide = el.querySelector(\".vide\");\n    return {\n      el,\n      cle: (data) => data.state.question,\n      jouable: () => true,\n      rendre(data) {\n        const q = data.state.question;\n        panneau.hidden = q == null;\n        vide.hidden = q != null;\n        ecrire(texte, q?.text ?? \"\");\n        ecrire(auteur, q?.author ?? \"\");\n        auteur.hidden = !q?.author;\n      }\n    };\n  }\n  function murHub(el) {\n    el.innerHTML = `\n    <header class=\"bande\"><h2 class=\"f-titre\" data-effet=\"mots\">Vos messages</h2></header>\n    <div class=\"contenu mur-hub\">\n      <div class=\"mur-hub-liste\" data-effet-enfants=\"entre\" data-cible=\".carte\" data-pas=\"90\" data-delai=\"200\"></div>\n      <div class=\"mur-hub-qr\" data-effet=\"pop\" data-delai=\"400\">\n        <div class=\"cadre-qr\"><svg viewBox=\"0 0 100 100\" fill=\"none\" stroke=\"#fff\" stroke-width=\"3.2\" stroke-linecap=\"round\"><path d=\"M3 28V13Q3 3 13 3H28M72 3H87Q97 3 97 13V28M97 72V87Q97 97 87 97H72M28 97H13Q3 97 3 87V72\"/></svg><div class=\"qr\"></div></div>\n        <p>Scannez pour laisser un message ou poser une question</p>\n      </div>\n    </div>`;\n    const liste = el.querySelector(\".mur-hub-liste\");\n    const colonne = el.querySelector(\".mur-hub-qr\");\n    const qr = el.querySelector(\".qr\");\n    return {\n      el,\n      cle: (data) => [data.state.comments, data.wall?.qrSvg],\n      jouable: () => true,\n      rendre(data) {\n        const messages = data.state.comments;\n        const node = data.wall?.qrSvg ? svg(data.wall.qrSvg) : null;\n        colonne.hidden = node == null;\n        qr.replaceChildren(...node ? [node] : []);\n        liste.replaceChildren(...messages.length === 0 ? [Object.assign(cree(\"article\", \"carte\"), { textContent: \"Les premiers messages appara\\xEEtront ici.\" })] : messages.map((m) => {\n          const carte = cree(\"article\", \"carte\");\n          carte.append(cree(\"p\", \"carte-nom\", m.author), cree(\"p\", \"carte-texte\", m.text));\n          return carte;\n        }));\n      }\n    };\n  }\n  function direct(el) {\n    return { el, cle: () => null, jouable: () => true, rendre() {\n    } };\n  }\n\n  // packages/projector/src/browser/sequence.ts\n  var ANNONCES = 4;\n  var PAGES_SPONSORS = 8;\n  var PLANNINGS = 6;\n  var range = (n) => Array.from({ length: n }, (_, i) => i);\n  var SCENES = [\n    { id: \"accueil\", nom: \"Accueil\", fabrique: accueil },\n    { id: \"agenda\", nom: \"Agenda\", fabrique: (el) => agenda(el) },\n    ...range(PLANNINGS).map((i) => ({\n      id: `planning-${i + 1}`,\n      nom: `Planning d'une autre salle ${i + 1}`,\n      fabrique: (el) => agenda(el, journeeAutre(i))\n    })),\n    ...range(ANNONCES).map((i) => ({ id: `annonce-${i + 1}`, nom: `Annonce ${i + 1}`, fabrique: (el) => annonce(el, i) })),\n    { id: \"merci\", nom: \"Merci \\xE0 nos sponsors\", fabrique: merci, signature: true },\n    ...range(PAGES_SPONSORS).map((i) => ({\n      id: `sponsors-${i + 1}`,\n      nom: `Sponsors, page ${i + 1}`,\n      fabrique: (el) => sponsors(el, i),\n      signature: true\n    })),\n    { id: \"posts\", nom: \"Mur social\", fabrique: mur },\n    { id: \"message-bienvenue\", nom: \"Message : bienvenue\", fabrique: (el) => message(el, \"bienvenue\"), signature: true },\n    { id: \"salles\", nom: \"Pendant ce temps\", fabrique: salles },\n    { id: \"agenda-rappel\", nom: \"Agenda (rappel)\", fabrique: (el) => agenda(el) },\n    { id: \"message-partage\", nom: \"Message : partage\", fabrique: (el) => message(el, \"partage\"), signature: true },\n    { id: \"reseaux\", nom: \"Nos r\\xE9seaux\", fabrique: reseaux },\n    { id: \"wallsio\", nom: \"Walls.io\", fabrique: wallsio },\n    { id: \"message-silence\", nom: \"Message : t\\xE9l\\xE9phones\", fabrique: (el) => message(el, \"silence\"), signature: true },\n    { id: \"conduite\", nom: \"Code de conduite\", fabrique: conduite, signature: true },\n    { id: \"feedbacks\", nom: \"Feedbacks\", fabrique: feedbacks, signature: true },\n    // The operator's screens.\n    { id: \"programme\", nom: \"Programme\", fabrique: programme },\n    { id: \"decompte\", nom: \"Compte \\xE0 rebours\", fabrique: decompte },\n    { id: \"banniere\", nom: \"Message\", fabrique: banniere },\n    { id: \"avis\", nom: \"Avis sur la conf\\xE9rence\", fabrique: avis },\n    { id: \"question\", nom: \"Question du public\", fabrique: question },\n    { id: \"mur-hub\", nom: \"Vos messages\", fabrique: murHub },\n    { id: \"direct\", nom: \"\\xC0 l\\u2019antenne\", fabrique: direct }\n  ];\n  var SCENE_IDS = SCENES.map((scene) => scene.id);\n  var DUREES = {\n    accueil: 10,\n    agenda: 20,\n    annonces: 8,\n    merci: 6,\n    sponsors: 8,\n    posts: 15,\n    \"message-bienvenue\": 7,\n    salles: 12,\n    \"agenda-rappel\": 20,\n    \"message-partage\": 7,\n    reseaux: 10,\n    wallsio: 25,\n    \"message-silence\": 7,\n    conduite: 14,\n    feedbacks: 10\n  };\n  var etape = (scene, groupe, ecran, transition = \"slide\", dureeTransition = 900) => ({ scene, duree: groupe == null ? 0 : DUREES[groupe], groupe, transition, dureeTransition, ecran });\n  var BOUCLE = [\n    etape(\"accueil\", \"accueil\", \"welcome\", \"stinger\", 1100),\n    etape(\"agenda\", \"agenda\", \"agenda\"),\n    // The other rooms' days, right after this one's: 15 s each unless the hub says otherwise.\n    ...range(PLANNINGS).map((i) => ({ ...etape(`planning-${i + 1}`, null, \"other-agendas\"), page: { de: \"plannings\", index: i } })),\n    ...range(ANNONCES).map((i) => etape(`annonce-${i + 1}`, \"annonces\", \"announcements\")),\n    etape(\"merci\", \"merci\", \"sponsors-thanks\"),\n    ...range(PAGES_SPONSORS).map((i) => ({ ...etape(`sponsors-${i + 1}`, \"sponsors\", \"sponsors\"), page: { de: \"sponsors\", index: i } })),\n    etape(\"posts\", \"posts\", \"posts\", \"slide\", 1e3),\n    etape(\"message-bienvenue\", \"message-bienvenue\", \"slogans\"),\n    etape(\"salles\", \"salles\", \"rooms\"),\n    // The room's day again, mid-loop, for whoever came in after the first pass.\n    // Withdrawn with the agenda, or on its own.\n    { ...etape(\"agenda-rappel\", \"agenda-rappel\", \"agenda\"), aussi: \"agenda-reminder\" },\n    etape(\"message-partage\", \"message-partage\", \"slogans\"),\n    etape(\"reseaux\", \"reseaux\", \"socials\"),\n    etape(\"wallsio\", \"wallsio\", \"wallsio\"),\n    etape(\"message-silence\", \"message-silence\", \"slogans\"),\n    etape(\"conduite\", \"conduite\", \"code-of-conduct\"),\n    etape(\"feedbacks\", \"feedbacks\", \"event-feedback\")\n  ];\n  function programmation(mode) {\n    const seul = (scene) => ({ etapes: [etape(scene, null, null)], tourne: false });\n    switch (mode) {\n      case \"loop\":\n        return { etapes: BOUCLE, tourne: true };\n      case \"sponsors\":\n        return {\n          etapes: [\n            etape(\"merci\", \"merci\", null),\n            ...range(PAGES_SPONSORS).map((i) => ({ ...etape(`sponsors-${i + 1}`, \"sponsors\", null), page: { de: \"sponsors\", index: i } }))\n          ],\n          tourne: true\n        };\n      case \"agenda\":\n        return seul(\"agenda\");\n      case \"wallsio\":\n        return seul(\"wallsio\");\n      case \"programme\":\n        return seul(\"programme\");\n      case \"countdown\":\n        return seul(\"decompte\");\n      case \"message\":\n        return seul(\"banniere\");\n      case \"feedback\":\n        return seul(\"avis\");\n      case \"question\":\n        return seul(\"question\");\n      case \"wall\":\n        return seul(\"mur-hub\");\n      case \"live\":\n        return { etapes: [etape(\"direct\", null, null, \"fade\", 450)], tourne: false };\n      default:\n        return { etapes: BOUCLE, tourne: true };\n    }\n  }\n\n  // packages/projector/src/browser/engine.ts\n  var ANIMEES_SUR_PLACE = /* @__PURE__ */ new Set([\"banniere\", \"question\", \"avis\"]);\n  var NOMS_TRANSITIONS = {\n    cut: \"Coupure\",\n    fade: \"Fondu\",\n    slide: \"Glissement\",\n    wipe: \"Balayage\",\n    zoom: \"Zoom\",\n    dip: \"Fondu au noir\",\n    stinger: \"Stinger\"\n  };\n  var EASE = \"cubic-bezier(.77, 0, .18, 1)\";\n  var prochaineImage = () => new Promise((r) => requestAnimationFrame(() => r()));\n  var attendre = (ms) => new Promise((r) => setTimeout(r, ms));\n  var Regie = class {\n    constructor(reglages) {\n      this.reglages = reglages;\n      for (const def of SCENES) {\n        const el = document.querySelector(`[data-scene=\"${def.id}\"]`);\n        if (el == null) continue;\n        if (def.signature) el.dataset.signature = \"\";\n        this.scenes.set(def.id, { def, scene: def.fabrique(el), cle: \"\", sale: true });\n      }\n      if (reglages.reduireAnimations) document.documentElement.classList.add(\"anim-reduites\");\n    }\n    reglages;\n    scenes = /* @__PURE__ */ new Map();\n    data = null;\n    mode = null;\n    prog = programmation(\"loop\");\n    courante = 0;\n    visible = null;\n    ecoule = 0;\n    enPause = false;\n    enTransition = false;\n    transitionForcee = null;\n    enAttente = null;\n    dernierTick = Date.now();\n    animBarre = null;\n    barre = document.querySelector(\"#progression i\");\n    voile = document.getElementById(\"voile\");\n    stinger = document.getElementById(\"stinger\");\n    logo = document.getElementById(\"logo\");\n    /** Called after every change the control panel shows. */\n    onChange = () => {\n    };\n    /* ================= Data ================= */\n    /** A new state: mark what changed, rebuild what is off screen, follow the mode. */\n    recevoir(data) {\n      const premier = this.data == null;\n      this.data = data;\n      document.body.dataset.mode = data.state.mode;\n      document.body.dataset.connectivity = data.state.connectivity;\n      if (data.eventIdentity?.name) document.title = `${data.eventIdentity.name} \\u2014 \\xE9cran de salle`;\n      this.majLogo(data);\n      this.majSignatures(data);\n      for (const m of this.scenes.values()) {\n        let cle;\n        try {\n          cle = JSON.stringify(m.scene.cle(data) ?? null);\n        } catch {\n          cle = String(Math.random());\n        }\n        if (cle !== m.cle) {\n          m.cle = cle;\n          m.sale = true;\n        }\n      }\n      if (premier) {\n        this.rendreSales(true);\n        this.mode = data.state.mode;\n        this.prog = programmation(data.state.mode);\n        this.courante = this.premiereJouable();\n        const depart = this.scenes.get(this.etape().scene);\n        depart.scene.el.classList.add(\"is-live\");\n        this.visible = depart;\n        this.barreDepart();\n        this.onChange();\n        return;\n      }\n      this.rendreSales(false);\n      if (data.state.mode !== this.mode) this.changerMode(data.state.mode);\n    }\n    /** The first scene to play, with its effects: after the loading screen lifts. */\n    lever(delai = 250) {\n      if (this.visible && this.reglages.effets) jouerEffets(this.visible.scene.el, delai);\n    }\n    etape(i = this.courante) {\n      return this.prog.etapes[i] ?? this.prog.etapes[0];\n    }\n    /** Seconds on screen: the hub's setting for its kind, or the reference's. */\n    duree(e = this.etape()) {\n      if (e == null) return 0;\n      if (e.page?.de === \"sponsors\") {\n        const own = this.data?.boucle?.sponsorPages[e.page.index]?.duree;\n        if (own != null) return own;\n      }\n      if (e.page?.de === \"plannings\") return this.data?.plannings[e.page.index]?.duree ?? 15;\n      return (e.groupe != null ? this.data?.boucle?.durees[e.groupe] : void 0) ?? e.duree;\n    }\n    estVisible(m) {\n      const cl = m.scene.el.classList;\n      return cl.contains(\"is-live\") || cl.contains(\"is-entering\");\n    }\n    /**\n     * Rebuilds the dirty scenes. Off screen always; in front of the room only the\n     * screen the operator put up — the loop never changes a scene under the eyes of\n     * the public.\n     */\n    rendreSales(tout) {\n      const data = this.data;\n      if (data == null) return;\n      for (const m of this.scenes.values()) {\n        if (!m.sale && !m.scene.sale) continue;\n        const visible = !tout && this.estVisible(m);\n        if (visible && this.prog.tourne) continue;\n        m.sale = false;\n        m.scene.sale = false;\n        try {\n          m.scene.rendre(data);\n          contours(m.scene.el);\n        } catch (cause) {\n          console.error(`Sc\\xE8ne \\xAB ${m.def.nom} \\xBB`, cause);\n        }\n        if (visible && ANIMEES_SUR_PLACE.has(m.def.id) && this.reglages.effets) jouerEffets(m.scene.el, 0);\n      }\n    }\n    /**\n     * Everything rebuilt — the R key, off screen only. `aussiVisible`: the scene on\n     * screen too, for the one moment it is allowed — under the loading screen, once\n     * the typefaces have arrived and every measurement is to be taken again.\n     */\n    relire(aussiVisible = false) {\n      for (const m of this.scenes.values()) m.sale = true;\n      this.rendreSales(aussiVisible);\n    }\n    majLogo(data) {\n      const url = data.boucle?.logoUrl ?? data.event?.logoUrl ?? null;\n      if (url) {\n        if (this.logo.getAttribute(\"src\") !== url) this.logo.src = url;\n        this.logo.alt = data.eventIdentity?.name ?? \"\";\n        this.logo.hidden = false;\n        this.logo.style.visibility = \"visible\";\n      } else {\n        this.logo.hidden = true;\n      }\n    }\n    majSignatures(data) {\n      const s = data.boucle?.signature ?? null;\n      for (const m of this.scenes.values()) {\n        if (!m.def.signature) continue;\n        let bloc = m.scene.el.querySelector(\":scope > .signature\");\n        if (!s || !s.texte) {\n          bloc?.remove();\n          continue;\n        }\n        if (!bloc) {\n          bloc = cree(\"div\", \"signature\");\n          bloc.dataset.effet = \"glisse\";\n          bloc.dataset.delai = \"600\";\n          m.scene.el.append(bloc);\n        }\n        const enfants = [];\n        if (s.icone === \"linkedin\") enfants.push(linkedin());\n        enfants.push(cree(\"span\", null, s.texte));\n        bloc.replaceChildren(...enfants);\n      }\n    }\n    /* ================= What plays ================= */\n    /** Withdrawn on the hub — only the loop listens to it. */\n    retiree(e) {\n      const retires = this.data?.screensDisabled ?? [];\n      return e.ecran != null && retires.includes(e.ecran) || e.aussi != null && retires.includes(e.aussi);\n    }\n    jouable(i) {\n      const e = this.prog.etapes[i];\n      if (e == null || this.data == null) return false;\n      if (!this.prog.tourne) return true;\n      if (this.retiree(e)) return false;\n      const m = this.scenes.get(e.scene);\n      if (m == null) return false;\n      try {\n        return m.scene.jouable(this.data);\n      } catch {\n        return false;\n      }\n    }\n    premiereJouable() {\n      for (let i = 0; i < this.prog.etapes.length; i++) if (this.jouable(i)) return i;\n      return 0;\n    }\n    voisine(depart, sens) {\n      const n = this.prog.etapes.length;\n      for (let k = 1; k <= n; k++) {\n        const i = ((depart + sens * k) % n + n) % n;\n        if (this.jouable(i)) return i;\n      }\n      return this.jouable(depart) ? depart : this.premiereJouable();\n    }\n    /**\n     * The operator changed the screen. Coming back to the loop always restarts at\n     * the welcome, with the stinger — never in the middle of the agenda, two\n     * seconds before the next switch.\n     */\n    changerMode(mode) {\n      this.mode = mode;\n      this.prog = programmation(mode);\n      this.courante = -1;\n      const cible = this.premiereJouable();\n      void this.allerA(cible);\n    }\n    /* ================= Transitions ================= */\n    anim(el, images, duration, easing = EASE) {\n      return el.animate(images, { duration, easing, fill: \"forwards\" }).finished;\n    }\n    TRANSITIONS = {\n      cut: async () => {\n      },\n      fade: ({ from, to, d }) => Promise.all([\n        this.anim(from, [{ opacity: 1 }, { opacity: 0 }], d, \"ease-in-out\"),\n        this.anim(to, [{ opacity: 0 }, { opacity: 1 }], d, \"ease-in-out\")\n      ]),\n      slide: ({ from, to, d }) => Promise.all([\n        this.anim(from, [{ transform: \"translateX(0)\" }, { transform: \"translateX(-100%)\" }], d),\n        this.anim(to, [{ opacity: 1, transform: \"translateX(100%)\" }, { opacity: 1, transform: \"translateX(0)\" }], d)\n      ]),\n      wipe: ({ from, to, d }) => Promise.all([\n        this.anim(from, [{ clipPath: \"inset(0 0 0 0%)\" }, { clipPath: \"inset(0 0 0 100%)\" }], d),\n        this.anim(to, [{ opacity: 1, clipPath: \"inset(0 100% 0 0)\" }, { opacity: 1, clipPath: \"inset(0 0% 0 0)\" }], d)\n      ]),\n      zoom: ({ from, to, d }) => Promise.all([\n        this.anim(from, [{ opacity: 1, transform: \"scale(1)\" }, { opacity: 0, transform: \"scale(.9)\" }], d),\n        this.anim(to, [{ opacity: 0, transform: \"scale(1.12)\" }, { opacity: 1, transform: \"scale(1)\" }], d)\n      ]),\n      dip: async ({ d, swap }) => {\n        await this.anim(this.voile, [{ opacity: 0 }, { opacity: 1 }], d / 2, \"ease-in\");\n        swap();\n        await this.anim(this.voile, [{ opacity: 1 }, { opacity: 0 }], d / 2, \"ease-out\");\n      },\n      stinger: async ({ d, swap }) => {\n        await this.anim(this.stinger, [\n          { transform: \"translateX(-2800px) skewX(-14deg)\" },\n          { transform: \"translateX(0) skewX(-14deg)\" }\n        ], d / 2, \"cubic-bezier(.55, 0, .9, .6)\");\n        swap();\n        await this.anim(this.stinger, [\n          { transform: \"translateX(0) skewX(-14deg)\" },\n          { transform: \"translateX(2800px) skewX(-14deg)\" }\n        ], d / 2, \"cubic-bezier(.1, .4, .45, 1)\");\n      }\n    };\n    get nomsTransitions() {\n      return Object.keys(this.TRANSITIONS);\n    }\n    async allerA(index, transition) {\n      const n = this.prog.etapes.length;\n      const i = (index % n + n) % n;\n      if (this.enTransition) {\n        this.enAttente = { i, transition };\n        return;\n      }\n      const vers = this.scenes.get(this.etape(i).scene);\n      if (vers == null) return;\n      const depuis = this.visible;\n      if (vers === depuis) {\n        this.courante = i;\n        this.ecoule = 0;\n        this.barreDepart();\n        this.onChange();\n        return;\n      }\n      if ((vers.sale || vers.scene.sale) && this.data) {\n        vers.sale = false;\n        vers.scene.sale = false;\n        try {\n          vers.scene.rendre(this.data);\n          contours(vers.scene.el);\n        } catch (cause) {\n          console.error(cause);\n        }\n      }\n      const e = this.etape(i);\n      let nom = transition ?? this.reglages.transition ?? this.transitionForcee ?? e.transition;\n      if (!this.TRANSITIONS[nom]) nom = \"fade\";\n      if (this.reglages.reduireAnimations && nom !== \"cut\") nom = \"fade\";\n      if (!peutAnimer() || depuis == null) nom = \"cut\";\n      const d = e.dureeTransition;\n      this.enTransition = true;\n      const avecVoile = nom === \"dip\" || nom === \"stinger\";\n      let bascule = false;\n      const swap = () => {\n        if (bascule) return;\n        bascule = true;\n        vers.scene.el.classList.add(\"is-live\");\n        depuis?.scene.el.classList.remove(\"is-live\");\n        if (avecVoile && this.reglages.effets) jouerEffets(vers.scene.el, 60);\n      };\n      const finir = () => {\n        swap();\n        vers.scene.el.classList.remove(\"is-entering\");\n        for (const el of [depuis?.scene.el, vers.scene.el, this.voile, this.stinger]) {\n          el?.getAnimations?.().forEach((a) => a.cancel());\n        }\n        this.courante = i;\n        this.visible = vers;\n        this.ecoule = 0;\n        this.enTransition = false;\n        this.barreDepart();\n        this.onChange();\n        if (depuis?.scene.quitte && this.data) {\n          const data = this.data;\n          setTimeout(() => {\n            if (!this.estVisible(depuis)) depuis.scene.quitte(data);\n          }, 150);\n        }\n        this.rendreSales(false);\n        const suite = this.enAttente;\n        this.enAttente = null;\n        if (suite) void this.allerA(suite.i, suite.transition);\n      };\n      vers.scene.el.classList.add(\"is-entering\");\n      if (nom === \"cut\") {\n        if (this.reglages.effets) jouerEffets(vers.scene.el, 0);\n        finir();\n        return;\n      }\n      await prochaineImage();\n      await prochaineImage();\n      this.barreVide(d);\n      if (!avecVoile && this.reglages.effets) jouerEffets(vers.scene.el, d * (DEBUT_EFFETS[nom] ?? 0));\n      try {\n        await Promise.race([this.TRANSITIONS[nom]({ from: depuis.scene.el, to: vers.scene.el, d, swap }), attendre(d + 800)]);\n      } catch (cause) {\n        console.error(\"Transition interrompue\", cause);\n      } finally {\n        finir();\n      }\n    }\n    suivante() {\n      void this.allerA(this.voisine(this.courante, 1));\n    }\n    precedente() {\n      void this.allerA(this.voisine(this.courante, -1));\n    }\n    /* ================= Progress bar ================= */\n    jouerBarre(images, duree, easing) {\n      if (this.barre == null) return;\n      const cadre = this.barre.parentElement;\n      cadre.hidden = !this.reglages.barreProgression || !this.prog.tourne;\n      if (cadre.hidden || !peutAnimer()) return;\n      this.animBarre?.cancel();\n      this.animBarre = this.barre.animate(images, { duration: Math.max(1, duree), easing, fill: \"forwards\" });\n      if (this.enPause) this.animBarre.pause();\n    }\n    barreDepart() {\n      this.jouerBarre([{ transform: \"scaleX(0)\" }, { transform: \"scaleX(1)\" }], this.duree() * 1e3, \"linear\");\n    }\n    barreVide(duree) {\n      this.jouerBarre([{ transform: \"scaleX(1)\" }, { transform: \"scaleX(0)\" }], duree, \"ease-in-out\");\n    }\n    basculerPause() {\n      this.enPause = !this.enPause;\n      if (this.animBarre) this.enPause ? this.animBarre.pause() : this.animBarre.play();\n      this.onChange();\n    }\n    /* ================= The clock ================= */\n    /**\n     * Every second: the scenes' clocks, then the loop moves on when the scene's\n     * time is up. A second is plenty for durations counted in seconds, and it\n     * keeps running where an OBS source slows its animation frames down.\n     */\n    tick() {\n      const data = this.data;\n      const t = Date.now();\n      const dt = Math.min(t - this.dernierTick, 5e3);\n      this.dernierTick = t;\n      if (data == null) return;\n      const now = maintenant(data);\n      for (const m of this.scenes.values()) {\n        if (!m.scene.tick) continue;\n        try {\n          m.scene.tick(data, now);\n        } catch (cause) {\n          console.error(`Sc\\xE8ne \\xAB ${m.def.nom} \\xBB`, cause);\n        }\n      }\n      this.rendreSales(false);\n      if (this.prog.tourne && !this.enPause && !this.enTransition) {\n        this.ecoule += dt;\n        if (this.ecoule >= this.duree() * 1e3) {\n          const suivante = this.voisine(this.courante, 1);\n          if (suivante === this.courante) this.ecoule = 0;\n          else void this.allerA(suivante);\n        }\n      }\n    }\n  };\n\n  // packages/projector/src/browser/hud.ts\n  var Hud = class {\n    constructor(regie) {\n      this.regie = regie;\n    }\n    regie;\n    el = document.getElementById(\"hud\");\n    liste = this.el.querySelector(\"[data-hud-liste]\");\n    barre = this.el.querySelector(\"[data-hud-barre]\");\n    etat = this.el.querySelector(\"[data-hud-etat]\");\n    transition = this.el.querySelector(\"[data-hud-transition]\");\n    infos = this.el.querySelector(\"[data-hud-infos]\");\n    prog = null;\n    basculer() {\n      this.el.hidden = !this.el.hidden;\n      document.body.classList.toggle(\"hud-on\", !this.el.hidden);\n      this.maj(true);\n    }\n    maj(complet) {\n      if (this.el.hidden) return;\n      const r = this.regie;\n      const etape2 = r.prog.etapes[r.courante];\n      const duree = r.duree(etape2);\n      this.barre.style.transform = `scaleX(${duree ? Math.min(1, r.ecoule / (duree * 1e3)) : 0})`;\n      if (!complet) return;\n      const cle = JSON.stringify(r.prog.etapes.map((e) => [e.scene, r.duree(e)]));\n      if (this.prog !== cle) {\n        this.prog = cle;\n        this.liste.replaceChildren(...r.prog.etapes.map((e, i) => {\n          const li = cree(\"li\", null, `${i + 1}. ${r.scenes.get(e.scene)?.def.nom ?? e.scene}`);\n          li.append(cree(\"span\", null, r.prog.tourne ? `${r.duree(e)} s, ${NOMS_TRANSITIONS[e.transition] ?? e.transition}` : \"tenu\"));\n          return li;\n        }));\n      }\n      ;\n      [...this.liste.children].forEach((li, i) => {\n        li.classList.toggle(\"est-live\", i === r.courante);\n        li.classList.toggle(\"est-vide\", !r.jouable(i));\n      });\n      this.etat.textContent = r.enPause ? \"En pause\" : \"Lecture\";\n      this.transition.textContent = r.transitionForcee ? `Transition forc\\xE9e : ${NOMS_TRANSITIONS[r.transitionForcee] ?? r.transitionForcee}` : \"Transition propre \\xE0 chaque sc\\xE8ne\";\n      const data = r.data;\n      const infos = [];\n      if (data) {\n        infos.push(`Mode : ${data.state.mode}`);\n        if (data.roomName) infos.push(`Salle : ${data.roomName}`);\n        infos.push(`Heure du hub : ${heureDans(maintenant(data), data.timezone)}`);\n        if (data.state.serverTimeOffsetMs && Math.abs(data.state.serverTimeOffsetMs) > 6e4) infos.push(\"horloge du hub d\\xE9cal\\xE9e\");\n        if (data.boucle?.wallsio.src) infos.push(`Walls.io : ${data.wallsIoReachable ? \"joignable\" : \"hors ligne, sc\\xE8ne saut\\xE9e\"}`);\n        if (data.screensDisabled.length) infos.push(`\\xC9crans retir\\xE9s : ${data.screensDisabled.join(\", \")}`);\n      }\n      this.infos.textContent = infos.join(\". \");\n    }\n  };\n\n  // packages/projector/src/browser/main.ts\n  function demarrer() {\n    const params = new URLSearchParams(location.search);\n    const reglages = {\n      effets: true,\n      barreProgression: true,\n      transition: null,\n      reduireAnimations: params.get(\"animations\") === \"reduites\",\n      ...window.__BOUCLE__ ?? {}\n    };\n    window.__BOUCLE_ARRET__?.abort();\n    const arret = new AbortController();\n    window.__BOUCLE_ARRET__ = arret;\n    const { signal } = arret;\n    const plateau = document.getElementById(\"stage\");\n    const ajuster = () => plateau.style.setProperty(\"--scale\", String(Math.min(innerWidth / 1920, innerHeight / 1080) || 1));\n    addEventListener(\"resize\", ajuster, { signal });\n    ajuster();\n    const regie = new Regie(reglages);\n    const barre = new BarreBas();\n    const hud = new Hud(regie);\n    regie.onChange = () => hud.maj(true);\n    let courant = null;\n    const recevoir = (data) => {\n      const premier = courant == null;\n      courant = data;\n      regie.recevoir(data);\n      barre.maj(data, true);\n      if (premier) void lever();\n    };\n    async function lever() {\n      const polices = document.fonts?.ready;\n      if (polices != null) {\n        await Promise.race([polices, new Promise((r) => setTimeout(r, 3e3))]);\n        regie.relire(true);\n      }\n      const chargement = document.getElementById(\"chargement\");\n      regie.lever();\n      if (chargement == null) return;\n      if (!peutAnimer() || !reglages.effets) {\n        chargement.remove();\n        return;\n      }\n      chargement.animate([{ opacity: 1 }, { opacity: 0 }], { duration: 500, easing: \"ease-out\", fill: \"forwards\" }).finished.then(() => chargement.remove(), () => chargement.remove());\n    }\n    setInterval(() => {\n      regie.tick();\n      if (courant) barre.maj(courant);\n      hud.maj(false);\n    }, 1e3);\n    const cycle = [null, ...regie.nomsTransitions];\n    addEventListener(\"keydown\", (event) => {\n      if (event.repeat || event.ctrlKey || event.metaKey || event.altKey) return;\n      const k = event.key.toLowerCase();\n      if (k === \"f\") {\n        event.preventDefault();\n        try {\n          if (document.fullscreenElement) void document.exitFullscreen?.();\n          else document.documentElement.requestFullscreen?.()?.catch(() => {\n          });\n        } catch {\n        }\n      } else if (k === \" \") {\n        event.preventDefault();\n        regie.basculerPause();\n      } else if (k === \"arrowright\") regie.suivante();\n      else if (k === \"arrowleft\") regie.precedente();\n      else if (/^[0-9]$/.test(k)) {\n        const n = k === \"0\" ? 10 : Number(k);\n        if (n <= regie.prog.etapes.length) void regie.allerA(n - 1);\n      } else if (k === \"t\") regie.transitionForcee = cycle[(cycle.indexOf(regie.transitionForcee) + 1) % cycle.length] ?? null;\n      else if (k === \"r\") regie.relire();\n      else if (k === \"h\") {\n        hud.basculer();\n        return;\n      }\n      hud.maj(true);\n    }, { signal });\n    let minuterie;\n    addEventListener(\"mousemove\", () => {\n      document.body.classList.add(\"curseur\");\n      clearTimeout(minuterie);\n      minuterie = setTimeout(() => document.body.classList.remove(\"curseur\"), 2e3);\n    }, { signal });\n    window.boucle = {\n      suivante: () => regie.suivante(),\n      precedente: () => regie.precedente(),\n      allerA: (numero, transition) => regie.allerA(numero - 1, transition),\n      pause: () => {\n        if (!regie.enPause) regie.basculerPause();\n      },\n      lecture: () => {\n        if (regie.enPause) regie.basculerPause();\n      },\n      relire: () => regie.relire(),\n      /**\n       * Fetches the state again at this address, every `ms` — the hub's public and\n       * global screens, which have no stream of their own. A failed fetch keeps the\n       * last state: the screen stays on what it knew.\n       */\n      suivre: (url, ms) => {\n        setInterval(() => {\n          fetch(url, { cache: \"no-store\", credentials: \"same-origin\" }).then((response) => response.ok ? response.json() : null).then((data) => {\n            if (data != null) recevoir(data);\n          }).catch(() => {\n          });\n        }, ms);\n      },\n      etat: () => ({\n        mode: regie.mode,\n        scene: regie.prog.etapes[regie.courante]?.scene ?? null,\n        courante: regie.courante + 1,\n        enPause: regie.enPause,\n        scenes: regie.prog.etapes.map((e, i) => ({ scene: e.scene, jouable: regie.jouable(i) }))\n      })\n    };\n    if (params.get(\"hud\") === \"1\") hud.basculer();\n    const embarque = document.getElementById(\"etat-initial\");\n    if (embarque?.textContent) recevoir(JSON.parse(embarque.textContent));\n    const patch = window.applyStreamPatch;\n    if (typeof EventSource !== \"undefined\" && !window.__PREVIEW__ && patch) {\n      const flux = new EventSource(\"/display/state?vue=projecteur&partiel=1\");\n      flux.onmessage = (event) => recevoir(JSON.parse(event.data));\n      flux.addEventListener(\"patch\", (event) => {\n        if (courant) recevoir(patch(courant, JSON.parse(event.data)));\n      });\n    }\n  }\n  demarrer();\n})();"

export const PROJECTOR_CSS = "/*\n * The reference loop's stylesheet, carried over verbatim from its index.html\n * (boucle-cloudnord, lines 45-701): colours, stage, decor, scenes, bottom band,\n * progress bar, ambient animations, HUD. Kept whole and untouched so that it can\n * be compared line for line with the design it comes from; what this screen adds\n * lives in the other files of this folder.\n *\n * The @font-face rules are not here: the room declares only the typefaces whose\n * files it has (see apps/room-client/src/core/fonts.ts).\n */\n:root {\n  /* Couleurs relevées dans la vidéo d'origine */\n  --violet: #5e17eb;\n  --magenta: #d420f8;\n  --cyan: #20d0dc;\n  --orange: #ffbd59;\n  --creme: #f4f0e4;\n  --marine: #2c2ca4;\n  --rose: #c868b4;\n  --degrade: linear-gradient(80deg, #9d3bdc 0%, #7636c8 25%, #5032b6 50%, #2a2ea4 75%, #112b99 100%);\n  --bulle: linear-gradient(90deg, #e21bfb, #14d3dc);\n\n  --f-titre: \"Peace Sans\", \"Arial Black\", \"Segoe UI Black\", sans-serif;\n  --f-texte: \"Open Sans\", \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif;\n  --f-accueil: \"Gagalin\", \"Peace Sans\", \"Arial Black\", sans-serif;\n  --f-script: \"Bukhari Script\", \"Segoe Script\", \"Brush Script MT\", cursive;\n\n  /* Bandeau du haut : logo à gauche, titre de la scène après --zone-logo */\n  --bande: 124px;\n  --marge: 48px;\n  --zone-logo: 470px;\n  /* Place réservée en bas pour le bandeau permanent (prochain talk, hashtag, heure) */\n  --barre-bas: 92px;\n}\n\n* { box-sizing: border-box; margin: 0; padding: 0; }\nhtml, body { width: 100%; height: 100%; overflow: hidden; background: #000; }\nbody { font-family: var(--f-texte); color: #fff; cursor: none; }\nbody.curseur, body.hud-on { cursor: default; }\n\n.f-titre { font-family: var(--f-titre); font-weight: 900; font-synthesis: none; }\n\n/* Texte avec contour épais, comme dans Canva : une copie du texte, tracée\n   derrière avec un trait large (data-texte est rempli automatiquement). */\n.contour { position: relative; z-index: 0; }\n.contour::before {\n  content: attr(data-texte);\n  position: absolute;\n  inset: 0;\n  z-index: -1;\n  color: var(--contour-couleur, var(--violet));\n  -webkit-text-stroke: var(--contour-epaisseur, .2em) var(--contour-couleur, var(--violet));\n}\n\n/* ---------- Plateau fixe 1920×1080, mis à l'échelle de la fenêtre ---------- */\n#stage {\n  position: absolute;\n  left: 50%;\n  top: 50%;\n  width: 1920px;\n  height: 1080px;\n  transform: translate(-50%, -50%) scale(var(--scale, 1));\n  overflow: hidden;\n  background: var(--degrade);\n}\n\n/* ---------- Décor commun à toutes les scènes (fixe pendant les transitions) ---------- */\n#decor { position: absolute; inset: 0; z-index: 0; pointer-events: none; }\n#decor svg { position: absolute; inset: 0; }\n.bulle {\n  position: absolute;\n  border-radius: 50%;\n  background: var(--bulle);\n  animation: flotte var(--d, 9s) ease-in-out var(--delai, 0s) infinite alternate;\n}\n@keyframes flotte {\n  from { transform: translate3d(0, -8px, 0); }\n  to   { transform: translate3d(0, 8px, 0); }\n}\n\n/* ---------- Scènes : transparentes, toutes montées, jamais en display:none ---------- */\n.scene {\n  position: absolute;\n  inset: 0;\n  z-index: 1;\n  opacity: 0;\n  pointer-events: none;\n  overflow: hidden;\n  backface-visibility: hidden;\n  contain: layout paint;\n}\n/* will-change seulement sur les deux scènes d'une transition : sinon chaque scène garde\n   une grande couche en mémoire vidéo. Elles sont préparées une image avant la transition. */\n.scene.is-live, .scene.is-entering { will-change: opacity, transform; }\n.scene.is-live { opacity: 1; z-index: 2; }\n.scene.is-entering { z-index: 3; }\n.scene > video { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }\n\n/* ---------- Logo permanent, signature ---------- */\n#logo {\n  position: absolute;\n  top: 26px;\n  left: var(--marge);\n  z-index: 5;\n  width: 400px;\n  height: 72px;\n  object-fit: contain;\n  object-position: left center;\n  visibility: hidden;\n}\n.signature {\n  position: absolute;\n  right: 30px;\n  bottom: calc(var(--barre-bas) + 14px);\n  display: flex;\n  align-items: center;\n  gap: 16px;\n  font: 700 42px var(--f-texte);\n}\n.signature img { width: 64px; height: 64px; }\n\n/* ---------- Bandeau du haut (agenda, mur) ---------- */\n.bande {\n  position: absolute;\n  top: 0;\n  left: 0;\n  right: 0;\n  height: var(--bande);\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 40px;\n  padding: 0 var(--marge) 0 var(--zone-logo);\n}\n.bande h2 {\n  min-width: 0;\n  font-size: 60px;\n  line-height: 1;\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n.bande-droite {\n  flex: none;\n  font: 800 56px var(--f-texte);\n  color: var(--orange);\n  font-variant-numeric: tabular-nums;\n  white-space: nowrap;\n}\n.contenu {\n  position: absolute;\n  top: var(--bande);\n  left: var(--marge);\n  right: var(--marge);\n  bottom: calc(var(--barre-bas) + 14px);\n}\n\n/* ---------- Rond blanc contenant un logo (sponsors, annonces) ---------- */\n.rond {\n  flex: none;\n  /* Sans min-width: 0, un rond qui affiche un nom au lieu d'un logo s'élargit à la taille\n     du texte, déforme la rangée et la fait passer à la ligne. */\n  min-width: 0;\n  min-height: 0;\n  /* Largeur ET hauteur imposées : un rond reste rond, quel que soit son voisin dans la rangée\n     (avec aspect-ratio seul, flexbox étirait certains ronds en ovale). */\n  width: calc(240px * var(--t, 1) * var(--k, 1));\n  height: calc(240px * var(--t, 1) * var(--k, 1));\n  display: grid;\n  place-items: center;\n  overflow: hidden;\n  border-radius: 50%;\n  background: #fff;\n}\n.rond img {\n  width: calc(var(--e, .7) * 100%);\n  height: calc(var(--e, .7) * 100%);\n  object-fit: contain;\n}\n.rond.sans-logo {\n  /* padding en em, pas en % : un pourcentage se calcule sur la largeur de la rangée,\n     ce qui gonflait le rond jusqu'à le rendre plus grand que ses voisins. */\n  padding: .6em;\n  font: 800 calc(26px * var(--t, 1) * var(--k, 1)) / 1.1 var(--f-texte);\n  text-align: center;\n  overflow-wrap: anywhere;\n  color: var(--violet);\n}\n\n/* ---------- Calques de transition ---------- */\n#voile { position: absolute; inset: 0; z-index: 10; opacity: 0; pointer-events: none; background: #1b0f55; }\n#stinger {\n  position: absolute;\n  top: 0;\n  bottom: 0;\n  left: -400px;\n  width: 2720px;\n  z-index: 10;\n  pointer-events: none;\n  transform: translateX(-2800px) skewX(-14deg);\n  will-change: transform;\n  background: linear-gradient(90deg,\n    var(--cyan) 0 10%, var(--violet) 10% 16%,\n    #e21bfb 16%, #8a6cf0 50%, #14d3dc 84%,\n    var(--violet) 84% 90%, var(--orange) 90% 100%);\n}\n\n/* ---------- Accueil ---------- */\n.accueil {\n  position: absolute;\n  inset: 0;\n  padding-bottom: var(--barre-bas);\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  gap: 120px;\n  padding-top: 30px;\n}\n.accueil-bienvenue {\n  font-family: var(--f-accueil);\n  font-weight: 900;\n  font-synthesis: none;\n  font-size: 112px;\n  line-height: 1;\n  text-transform: uppercase;\n  letter-spacing: .03em;\n}\n.accueil-logo { width: 1780px; max-width: 93%; visibility: hidden; }\n\n/* ---------- Messages en typographie animée ---------- */\n.message {\n  position: absolute;\n  top: var(--bande);\n  left: var(--marge);\n  right: var(--marge);\n  bottom: calc(var(--barre-bas) + 18px);\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  gap: 34px;\n  text-align: center;\n}\n.message-texte { font-size: 150px; line-height: 1.04; white-space: pre-line; }\n.message-sous { font: 800 56px / 1.2 var(--f-texte); color: var(--orange); white-space: pre-line; }\n.message-sous:empty { display: none; }\n/* Style « arcenciel » : lettres blanches à ombre violette, parcourues par une vague de couleurs */\n.arcenciel { text-shadow: 0 .05em 0 var(--violet), 0 .1em .12em rgba(27, 15, 85, .45); }\n.vague.arcenciel .morceau {\n  animation: vague 3.4s ease-in-out infinite, teinte 3.6s linear infinite;\n  animation-delay: calc(var(--i, 0) * -.22s), calc(var(--i, 0) * -.09s);\n}\n@keyframes teinte {\n  0%, 55%, 100% { color: #fff; }\n  65% { color: var(--orange); }\n  75% { color: #ff8bf5; }\n  85% { color: #8ef4ff; }\n}\n.deux-points { animation: deux-points 1s steps(1) infinite; }\n@keyframes deux-points { 50% { opacity: .25; } }\n\n/* ---------- Grand titre seul (« Merci à nos Sponsors ») ---------- */\n.titre-plein { position: absolute; inset: 0; padding-bottom: var(--barre-bas); display: grid; place-items: center; }\n.titre-plein h2 { font-size: 190px; line-height: 1.02; text-align: center; white-space: pre-line; }\n\n/* ---------- Annonce « offert par » ---------- */\n.annonce {\n  position: absolute;\n  top: var(--bande);\n  left: 0;\n  right: 0;\n  bottom: calc(var(--barre-bas) + 10px);\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  text-align: center;\n}\n.annonce-titre { font-size: 138px; line-height: 1; white-space: pre-line; }\n.annonce-sous { margin-top: 8px; font-size: 92px; line-height: 1.05; }\n.annonce-logos { display: flex; align-items: center; gap: 60px; margin-top: 44px; }\n.annonce-logos .rond { --t: 1.375; }\n\n/* ---------- Pages sponsors ---------- */\n.sp {\n  position: absolute;\n  top: var(--bande);\n  left: var(--marge);\n  right: var(--marge);\n  bottom: calc(var(--barre-bas) + 28px);\n  display: flex;\n  flex-direction: column;\n}\n.sp-titre { margin-bottom: 36px; font-size: 110px; line-height: .98; text-align: center; white-space: pre-line; }\n.sp-titre:empty { display: none; }\n.sp-zone { flex: 1; min-height: 0; display: flex; align-items: center; overflow: hidden; }\n.sp-rangs { width: 100%; display: flex; flex-direction: column; align-items: center; gap: calc(80px * var(--k, 1)); }\n.sp-rang {\n  display: flex;\n  flex-wrap: wrap;\n  justify-content: center;\n  align-items: center;\n  gap: calc(60px * var(--k, 1)) calc(90px * var(--k, 1));\n}\n\n/* ---------- Code de conduite ---------- */\n.cc-panneau {\n  position: absolute;\n  left: 293px;\n  right: 314px;\n  top: 130px;\n  padding: 36px 50px;\n  display: flex;\n  flex-direction: column;\n  gap: 1.25em;\n  border-radius: 18px;\n  background: rgba(255, 255, 255, .3);\n  font: 800 39px / 1.3 var(--f-texte);\n  text-align: center;\n  --contour-couleur: var(--marine);\n  --contour-epaisseur: .15em;\n}\n.cc-panneau p { white-space: pre-line; }\n.cc-qr { position: absolute; left: 14px; bottom: calc(var(--barre-bas) + 16px); display: flex; align-items: center; }\n.cc-legende { width: 380px; font: 700 33px / 1.3 var(--f-texte); text-align: center; white-space: pre-line; }\n.cadre-qr { position: relative; width: 222px; height: 222px; }\n.cadre-qr svg { position: absolute; inset: 0; }\n.cadre-qr img { position: absolute; inset: 12%; width: 76%; height: 76%; }\n\n/* Slogans inclinés (« Passez une bonne conférence », « Pensez à donner vos feedbacks ») */\n.slogan {\n  position: absolute;\n  left: var(--x);\n  top: var(--y);\n  transform: translate(-50%, -50%) rotate(-11deg);\n  font: 800 78px / 1.02 var(--f-texte);\n  text-align: center;\n  text-transform: uppercase;\n  white-space: nowrap;\n}\n.slogan-creux { color: transparent; -webkit-text-stroke: 3px #fff; }\n.slogan-orange { color: var(--orange); white-space: pre; --contour-epaisseur: .12em; }\n.cc-slogan { --x: 1256px; --y: 812px; font-size: 68px; }\n\n/* ---------- Feedbacks ---------- */\n.fb-slogan { --x: 690px; --y: 200px; }\n.fb-qr { position: absolute; left: 456px; top: 380px; width: 410px; }\n.fb-merci {\n  position: absolute;\n  left: 1513px;\n  top: 606px;\n  transform: translate(-50%, -50%) rotate(-14deg);\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  font-family: var(--f-script);\n  line-height: .95;\n  color: var(--creme);\n  text-shadow: 4px 4px 0 #e0585c, 8px 8px 0 #b44c70;\n  white-space: nowrap;\n}\n.fb-merci span:first-child { font-size: 190px; }\n.fb-merci span:last-child { margin-left: 120px; font-size: 128px; }\n\n/* ---------- Agenda : deux colonnes, taille de texte calculée pour remplir l'espace ---------- */\n.agenda {\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);\n  column-gap: 36px;\n  font-size: var(--fs, 32px);\n  overflow: hidden;\n}\n.agenda-col { list-style: none; display: flex; flex-direction: column; gap: .28em; }\n.seance {\n  position: relative;\n  display: grid;\n  grid-template-columns: 3.2em minmax(0, 1fr);\n  column-gap: .6em;\n  padding: .4em .6em .48em .75em;\n  border-radius: .2em;\n  background: rgba(30, 16, 110, .72); /* assez dense pour que les bulles du décor ne gênent pas la lecture */\n  overflow: hidden;\n}\n.seance::before {\n  content: \"\";\n  position: absolute;\n  left: 0;\n  top: 0;\n  bottom: 0;\n  width: .16em;\n}\n.seance-heure strong {\n  display: block;\n  font-weight: 800;\n  line-height: 1.12;\n  color: var(--orange);\n  font-variant-numeric: tabular-nums;\n}\n.seance-heure span {\n  display: block;\n  font-size: .6em;\n  font-weight: 700;\n  color: rgba(255, 255, 255, .72);\n  font-variant-numeric: tabular-nums;\n}\n.seance-titre { font-weight: 800; line-height: 1.14; }\n.seance-meta { margin-top: .28em; font-size: .6em; font-weight: 700; line-height: 1.3; color: #c6f3f7; }\n.pastille {\n  display: inline-block;\n  margin-right: .5em;\n  padding: .02em .45em .08em;\n  border-radius: .25em;\n  background: var(--violet);\n  color: #fff;\n}\n/* Session qui a lieu dans une autre salle (keynote en plénière) */\n.pastille-lieu { background: var(--orange); color: #2a1470; }\n.pastille-langue { background: rgba(255, 255, 255, .22); }\n.seance.est-encours .pastille:not(.pastille-lieu) { background: rgba(20, 8, 80, .55); }\n.seance.est-pause { background: transparent; padding-top: .16em; padding-bottom: .16em; }\n.seance.est-pause .seance-heure strong,\n.seance.est-pause .seance-titre { font-size: .66em; font-weight: 700; color: rgba(255, 255, 255, .75); line-height: 1.3; }\n.seance.est-pause .seance-heure span { display: none; }\n.seance-avance {\n  position: absolute;\n  left: 0;\n  bottom: 0;\n  width: 100%;\n  height: .13em;\n  background: var(--bulle);\n  transform-origin: left;\n  transform: scaleX(var(--avance, 0));\n}\n.seance.est-passee { opacity: .4; }\n.seance.est-encours { background: rgba(94, 23, 235, .94); }\n.seance.est-encours::before { background: linear-gradient(180deg, #e21bfb, #14d3dc); }\n.seance.est-pause.est-encours { background: rgba(30, 16, 110, .5); }\n.seance.est-suivante::before { background: var(--orange); }\n\n/* ---------- Mur social ---------- */\n.mur { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); column-gap: 28px; }\n.mur-col { position: relative; display: flex; flex-direction: column; gap: 24px; overflow: hidden; }\n.carte {\n  flex: none;\n  padding: 26px 28px 20px;\n  border-radius: 18px;\n  background: #fff;\n  color: #1d1747;\n  box-shadow: 0 10px 30px rgba(26, 12, 90, .25);\n}\n.carte-tete {\n  display: grid;\n  grid-template-columns: 68px minmax(0, 1fr) auto;\n  gap: 16px;\n  align-items: center;\n  margin-bottom: 16px;\n}\n.avatar {\n  width: 68px;\n  height: 68px;\n  overflow: hidden;\n  display: grid;\n  place-items: center;\n  border-radius: 50%;\n  font-size: 26px;\n  font-weight: 800;\n  color: #fff;\n  background: var(--c, var(--violet));\n}\n.avatar img { width: 100%; height: 100%; object-fit: cover; }\n.carte-nom { font-size: 27px; font-weight: 800; line-height: 1.15; }\n.carte-titre, .carte-date { font-size: 20px; font-weight: 700; color: #6b6590; }\n.carte-titre { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n.carte-date { align-self: start; white-space: nowrap; }\n.carte-texte {\n  font-size: 25px;\n  font-weight: 700;\n  line-height: 1.42;\n  white-space: pre-line;\n  display: -webkit-box;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: 9;\n  overflow: hidden;\n}\n.carte.avec-image .carte-texte { -webkit-line-clamp: 5; }\n.carte-texte .tag { color: var(--violet); font-weight: 800; }\n.carte-image {\n  display: block;\n  width: 100%;\n  aspect-ratio: 16 / 9;\n  object-fit: cover;\n  margin-top: 16px;\n  border-radius: 10px;\n}\n.carte-pied {\n  display: flex;\n  justify-content: space-between;\n  gap: 16px;\n  margin-top: 16px;\n  padding-top: 12px;\n  border-top: 1px solid #e6e2f3;\n  font-size: 20px;\n  font-weight: 700;\n  color: #6b6590;\n}\n\n/* ---------- Bandeau permanent du bas : prochain talk, hashtag, heure ---------- */\n#barre-bas {\n  position: absolute;\n  left: 0;\n  right: 0;\n  bottom: 8px;\n  z-index: 6;\n  height: calc(var(--barre-bas) - 8px);\n  display: grid;\n  grid-template-columns: minmax(0, 1fr) auto auto;\n  align-items: center;\n  gap: 40px;\n  padding: 0 40px 0 var(--marge);\n  background: linear-gradient(90deg, rgba(18, 9, 64, .78), rgba(18, 9, 64, .55));\n  border-top: 1px solid rgba(255, 255, 255, .16);\n}\n#barre-bas[hidden] { display: none; }\n#barre-bas [hidden] { display: none; } /* sinon .pastille (inline-block) l'emporte sur [hidden] */\n.bb-prochain { min-width: 0; display: flex; align-items: baseline; gap: 20px; }\n.bb-libelle { flex: none; font: 700 26px var(--f-texte); color: var(--ciel); }\n.bb-heure { flex: none; font: 800 40px var(--f-texte); color: var(--orange); font-variant-numeric: tabular-nums; }\n.bb-titre { min-width: 0; font: 800 34px var(--f-texte); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n.bb-prochain .pastille { flex: none; font: 700 24px var(--f-texte); }\n.bb-partage { flex: none; display: flex; align-items: baseline; gap: 16px; }\n.bb-message { font: 700 28px var(--f-texte); color: rgba(255, 255, 255, .78); }\n.bb-hashtag { font: 800 40px var(--f-texte); color: var(--orange); }\n.bb-horloge { flex: none; font: 800 46px var(--f-texte); color: #fff; font-variant-numeric: tabular-nums; }\n\n/* ---------- Barre de progression de la scène ---------- */\n#progression {\n  position: absolute;\n  left: 0;\n  right: 0;\n  bottom: 0;\n  z-index: 6;\n  height: 8px;\n  background: rgba(255, 255, 255, .14);\n}\n#progression i {\n  display: block;\n  height: 100%;\n  background: linear-gradient(90deg, #e21bfb, #14d3dc, #ffbd59);\n  box-shadow: 0 0 14px rgba(226, 27, 251, .7);\n  transform-origin: left;\n  transform: scaleX(0);\n}\n\n/* ---------- Walls.io ---------- */\n/* Le mur est affiché plus petit puis agrandi (zoom dans donnees.js) : Walls.io met\n   moins de colonnes et tout devient lisible de loin. */\n.wallsio { border-radius: 18px; overflow: hidden; }\n.wallsio iframe {\n  position: absolute;\n  left: 0;\n  top: 0;\n  width: calc(100% / var(--zoom-wallsio, 1));\n  height: calc(100% / var(--zoom-wallsio, 1));\n  border: 0;\n  background: transparent;\n  transform: scale(var(--zoom-wallsio, 1));\n  transform-origin: 0 0;\n}\n/* Intégration JavaScript : le widget Walls.io pose ses propres styles en ligne sur l'iframe,\n   il faut donc reprendre la main avec !important. */\n.wallsio.js iframe {\n  position: absolute !important;\n  left: 0 !important;\n  top: 0 !important;\n  width: calc(100% / var(--zoom-wallsio, 1)) !important;\n  height: calc(100% / var(--zoom-wallsio, 1)) !important;\n  max-width: none !important;\n  border: 0 !important;\n}\n\n/* ---------- Animations d'ambiance : en continu, pour que rien ne reste figé ----------\n   Elles utilisent translate / scale / rotate, qui s'ajoutent aux effets d'apparition\n   (animés, eux, avec transform) sans les perturber. */\n.vague .morceau { animation: vague 3.4s ease-in-out infinite; animation-delay: calc(var(--i, 0) * -.22s); }\n@keyframes vague { 0%, 100% { translate: 0 0; } 50% { translate: 0 -.07em; } }\n.respire { animation: respire 2.8s ease-in-out infinite; }\n@keyframes respire { 0%, 100% { scale: 1; } 50% { scale: 1.045; } }\n.flotte { animation: flotte-doux 5s ease-in-out infinite alternate; animation-delay: calc(var(--i, 0) * -1.3s); }\n@keyframes flotte-doux { from { translate: 0 -7px; } to { translate: 0 7px; } }\n.balance { animation: balance 4.2s ease-in-out infinite; animation-delay: calc(var(--i, 0) * -1.4s); }\n@keyframes balance { 0%, 100% { rotate: -2.5deg; } 50% { rotate: 2.5deg; } }\n/* Halo pulsé : une opacité animée, gérée par la carte graphique, plutôt qu'une ombre\n   recalculée à chaque image. */\n.seance.est-encours::after {\n  content: \"\";\n  position: absolute;\n  inset: 0;\n  pointer-events: none;\n  border-radius: inherit;\n  box-shadow: inset 0 0 40px rgba(226, 27, 251, .85);\n  animation: pulse-opacite 2.6s ease-in-out infinite;\n}\n@keyframes pulse-opacite { 0%, 100% { opacity: 0; } 50% { opacity: 1; } }\n.seance.est-suivante::before { animation: clignote 1.6s ease-in-out infinite; }\n@keyframes clignote { 50% { opacity: .35; } }\n\n/* ---------- Effets d'apparition : morceaux de texte animés un par un ---------- */\n.morceau, .mot-lettres { display: inline-block; }\n.mot-lettres { white-space: nowrap; }\n.contour.decoupe::before { content: none; } /* le contour passe sur chaque mot */\n\n/* Les animations d'ambiance ne tournent que sur la scène affichée. En pause ailleurs :\n   même rendu, beaucoup moins de calcul (une quinzaine de scènes sont montées en permanence). */\n.scene:not(.is-live):not(.is-entering) :is(.vague .morceau, .respire, .flotte, .balance, .deux-points) { animation-play-state: paused; }\n.scene:not(.is-live):not(.is-entering) .seance.est-encours::after,\n.scene:not(.is-live):not(.is-entering) .seance.est-suivante::before { animation-play-state: paused; }\n\n/* Seulement si respecterReductionAnimations est activé (voir le script). */\n.anim-reduites .bulle, .anim-reduites .vague .morceau, .anim-reduites .respire, .anim-reduites .flotte,\n.anim-reduites .balance, .anim-reduites .seance.est-encours::after, .anim-reduites .seance.est-suivante::before,\n.anim-reduites .deux-points { animation: none; }\n\n/* ---------- Écran de démarrage (le seul moment de chargement) ---------- */\n#chargement {\n  position: fixed;\n  inset: 0;\n  z-index: 50;\n  display: grid;\n  place-items: center;\n  padding: 40px;\n  background: var(--degrade);\n  color: rgba(255, 255, 255, .8);\n  font-size: 22px;\n  font-weight: 700;\n  line-height: 1.5;\n  text-align: center;\n}\n#chargement p { max-width: 760px; }\n\n/* ---------- Panneau de contrôle (touche H) ---------- */\n#hud {\n  position: fixed;\n  left: 20px;\n  bottom: 20px;\n  z-index: 100;\n  width: 420px;\n  padding: 16px 18px 14px;\n  background: rgba(27, 15, 85, .95);\n  border: 1px solid rgba(255, 255, 255, .25);\n  border-radius: 8px;\n  font-size: 15px;\n  line-height: 1.4;\n}\n#hud[hidden] { display: none; }\n.hud-etat { display: flex; justify-content: space-between; gap: 12px; margin-bottom: 6px; }\n.hud-etat strong { color: var(--orange); }\n.hud-etat span, .hud-infos { color: rgba(255, 255, 255, .7); }\n.hud-infos { margin-bottom: 10px; font-size: 13px; }\n#hud ol { list-style: none; margin-bottom: 12px; }\n#hud li { display: flex; justify-content: space-between; gap: 12px; padding: 2px 8px; border-radius: 4px; }\n#hud li span { color: rgba(255, 255, 255, .6); }\n#hud li.est-live { background: rgba(255, 189, 89, .22); }\n#hud li.est-vide { opacity: .45; }\n.hud-barre { height: 4px; margin-bottom: 12px; overflow: hidden; background: rgba(255, 255, 255, .2); }\n.hud-barre i { display: block; height: 100%; background: var(--bulle); transform-origin: left; transform: scaleX(0); }\n.hud-aide { font-size: 13px; color: rgba(255, 255, 255, .7); }\n\n/*\n * What this screen adds to the reference loop, in the reference's own idiom:\n * the two pages it did not have (\"Pendant ce temps\", \"Nos réseaux\"), the screens\n * the operator puts up from the control room, and the room's state in the bottom\n * band. Same stage, same px, same palette — nothing here overrides the reference.\n */\n\n/* The free stand-ins for the reference's two non-free faces (see\n   packages/projector/src/server/fonts.ts): Luckiest Guy for Gagalin, Pacifico\n   for Bukhari Script. The originals stay first in line, for a machine that\n   has them installed under a licence of its own. */\n:root {\n  --f-accueil: \"Gagalin\", \"Luckiest Guy\", \"Peace Sans\", \"Arial Black\", sans-serif;\n  --f-script: \"Bukhari Script\", \"Pacifico\", \"Segoe Script\", \"Brush Script MT\", cursive;\n}\n\n/* The event's logo is the one image of the frame that may be missing. */\n#logo[hidden] { display: none; }\n\n/* A scene's message when it has nothing to show — only in the modes the operator\n   chose; the loop skips an empty scene instead. */\n.vide {\n  position: absolute;\n  inset: var(--bande) var(--marge) calc(var(--barre-bas) + 14px);\n  display: grid;\n  place-items: center;\n  font: 800 56px / 1.2 var(--f-texte);\n  color: rgba(255, 255, 255, .78);\n  text-align: center;\n  white-space: pre-line;\n}\n.vide[hidden] { display: none; }\n\n/* ---------- Signature: the LinkedIn mark, drawn rather than shipped ---------- */\n.signature svg { width: 64px; height: 64px; flex: none; }\n\n/* ---------- Bottom band: the room's break and its link to the hub ---------- */\n.bb-etat { flex: none; display: flex; align-items: center; gap: 18px; }\n.bb-etat .pastille { font: 700 24px var(--f-texte); }\n.bb-etat .pastille[hidden] { display: none; }\n.pastille-pause { background: var(--orange); color: #2a1470; }\n.pastille-pause.a-venir { background: rgba(255, 255, 255, .22); color: var(--orange); }\n/* Discreet on purpose: it speaks to the technician, not to the room. */\n.bb-lien { width: 16px; height: 16px; border-radius: 50%; background: #f2b33d; }\n.bb-lien[hidden] { display: none; }\nbody[data-connectivity=\"OFFLINE\"] .bb-lien { background: #ef5a5a; }\n#barre-bas { grid-template-columns: minmax(0, 1fr) auto auto auto; }\n\n/* ---------- \"Pendant ce temps, à côté\" and the hub's wall: white cards ---------- */\n.salles { display: grid; grid-template-columns: repeat(var(--n, 2), minmax(0, 1fr)); gap: 32px; align-content: center; }\n.salle .carte-tete { grid-template-columns: minmax(0, 1fr) auto; margin-bottom: 18px; }\n.salle-nom { font-size: 30px; font-weight: 800; color: var(--violet); }\n.salle-heure { font: 800 40px var(--f-texte); color: #c77414; font-variant-numeric: tabular-nums; white-space: nowrap; }\n.salle-heure.maintenant { color: var(--violet); }\n.salle-titre { font-size: 40px; font-weight: 800; line-height: 1.18; }\n.salle-qui { margin-top: 14px; font-size: 26px; font-weight: 700; color: #6b6590; }\n\n/* ---------- \"Nos réseaux\" ---------- */\n.reseaux { display: flex; flex-wrap: wrap; align-content: center; justify-content: center; gap: 40px 48px; }\n.reseau { min-width: 520px; padding: 36px 48px 40px; text-align: center; }\n.reseau-nom { font: 800 30px var(--f-texte); letter-spacing: .12em; text-transform: uppercase; color: var(--violet); }\n.reseau-compte { margin-top: 14px; font: 800 64px / 1.05 var(--f-texte); color: #1d1747; overflow-wrap: anywhere; }\n\n/* ---------- Programme: the day in one column, walking round ---------- */\n.scroller { overflow: hidden; }\n.programme-liste { list-style: none; display: flex; flex-direction: column; gap: .28em; font-size: 40px; }\n.cycling {\n  animation-name: program-cycle;\n  animation-timing-function: cubic-bezier(.4, 0, .2, 1);\n  animation-iteration-count: infinite;\n  animation-fill-mode: both;\n}\n@keyframes program-cycle {\n  0%, 14% { transform: translateY(var(--from)); }\n  38%, 52% { transform: translateY(var(--end)); }\n  76%, 90% { transform: translateY(0); }\n  100% { transform: translateY(var(--from)); }\n}\n\n/* ---------- Countdown ---------- */\n.decompte {\n  position: absolute;\n  inset: var(--bande) var(--marge) calc(var(--barre-bas) + 14px);\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  gap: 30px;\n  text-align: center;\n}\n.decompte-titre { font-size: 96px; line-height: 1; }\n.decompte-chiffres { font-size: 330px; line-height: .95; font-variant-numeric: tabular-nums; --contour-epaisseur: .12em; }\n.decompte-reprise { font: 800 56px / 1.2 var(--f-texte); color: var(--orange); }\n.beat { animation: beat .5s ease-out; }\n@keyframes beat { from { scale: 1.035; } to { scale: 1; } }\n\n/* ---------- The console's message, on screen ---------- */\n.message.urgent { padding: 60px; border-radius: 28px; background: rgba(122, 20, 32, .88); }\n.message.warning .message-texte { color: var(--orange); }\n\n/* ---------- The running talk's feedback QR ---------- */\n.avis {\n  position: absolute;\n  inset: var(--bande) var(--marge) calc(var(--barre-bas) + 14px);\n  display: grid;\n  grid-template-columns: auto minmax(0, 1fr);\n  align-items: center;\n  gap: 90px;\n  padding: 0 60px;\n}\n.avis .cadre-qr { width: 520px; height: 520px; }\n.avis .cadre-qr .qr { position: absolute; inset: 12%; background: #fff; border-radius: 12px; padding: 14px; }\n.avis .cadre-qr .qr svg { display: block; width: 100%; height: 100%; }\n.avis-titre { font-size: 84px; line-height: 1.02; }\n.avis-talk { margin-top: 36px; font: 800 48px / 1.2 var(--f-texte); }\n.avis-aide { margin-top: 24px; font: 700 34px / 1.35 var(--f-texte); color: rgba(255, 255, 255, .8); }\n\n/* QR codes drawn by the room (code of conduct, feedbacks): white square in the\n   reference's rounded frame, where the reference had an image. */\n.cadre-qr .qr { position: absolute; inset: 12%; background: #fff; border-radius: 8px; padding: 6px; }\n.cadre-qr .qr svg { display: block; width: 100%; height: 100%; }\n.fb-qr.cadre-qr { width: 410px; height: 410px; }\n\n/* ---------- Audience question ---------- */\n.question {\n  position: absolute;\n  left: 200px;\n  right: 200px;\n  top: 190px;\n  bottom: calc(var(--barre-bas) + 60px);\n  display: flex;\n  flex-direction: column;\n  justify-content: center;\n  gap: 40px;\n  padding: 60px 80px;\n  border-radius: 18px;\n  background: rgba(255, 255, 255, .3);\n  text-align: center;\n  --contour-couleur: var(--marine);\n  --contour-epaisseur: .15em;\n}\n.question-texte { font: 800 64px / 1.25 var(--f-texte); white-space: pre-line; }\n.question-auteur { font: 800 40px var(--f-texte); color: var(--orange); }\n\n/* ---------- The hub's moderated wall ---------- */\n.mur-hub { display: grid; grid-template-columns: minmax(0, 1fr) 380px; gap: 48px; align-items: start; }\n.mur-hub-liste { display: flex; flex-direction: column; gap: 22px; overflow: hidden; max-height: 100%; }\n.mur-hub .carte-texte { -webkit-line-clamp: 4; font-size: 32px; }\n.mur-hub-qr { display: flex; flex-direction: column; align-items: center; gap: 22px; }\n.mur-hub-qr .cadre-qr { width: 340px; height: 340px; }\n.mur-hub-qr p { font: 700 30px / 1.3 var(--f-texte); text-align: center; }\n\n/* ---------- On air: the stage goes black, the band leaves ---------- */\nbody[data-mode=\"live\"] #stage { background: #000; }\nbody[data-mode=\"live\"] #decor,\nbody[data-mode=\"live\"] #logo,\nbody[data-mode=\"live\"] #barre-bas,\nbody[data-mode=\"live\"] #progression { opacity: 0; transition: opacity .45s ease; }\n\n/* The welcome's logo in a fixed box: the reference's is a wide wordmark, the\n   program's may well be square — contained, it can no longer push the\n   « Bienvenue à » off the stage. */\n.accueil-logo { height: 600px; object-fit: contain; }\n\n/* `hidden` always wins: the browser's own `[hidden] { display: none }` loses to\n   the slightest author rule setting a display — a trap met on the console. */\n[hidden] { display: none !important; }\n\n/* ---------- One clock on screen ----------\n   The agenda and the programme carried the time top right, as in the reference,\n   and the permanent band carries it bottom right: the same minute twice. The top\n   one only shows when the band is switched off. */\nbody:not(.sans-barre-bas) .bande-droite[data-horloge] { display: none; }\n\n/* ---------- « Vous êtes ici » ----------\n   The loop now shows the other rooms' days too: this room's says so, next to\n   its name, in the orange the plenaries' room pill already uses. */\n.bande-titre { min-width: 0; display: flex; align-items: center; gap: 26px; }\n.pastille-ici {\n  flex: none;\n  margin: 0;\n  padding: .12em .6em .18em;\n  font: 800 30px var(--f-texte);\n  background: var(--orange);\n  color: #2a1470;\n}"
